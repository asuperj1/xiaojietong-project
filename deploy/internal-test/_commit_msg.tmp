feat(deploy): 内部联调部署方案（公网 IP + 8080）+ 访问闸门 + 合规校验工具

背景：ICP 备案审核中，需让 4 人团队从公网联调，但**绝不能对外提供网站服务**。
因此新增一套严格受限的部署方案，并用工具把约束固化下来。

## 硬约束（全部落实）

- 后端固定监听 8080；**无任何 80/443 配置**
- **不绑域名、不装 Nginx、不申请证书、不做内网穿透**
- 3306 / 6379 仅回环；安全组与主机防火墙双层拒绝 80/443
- 新增「访问闸门」简单鉴权，挡住外网陌生人（XJT_ACCESS_TOKEN）

## 第一部分 · 部署 Shell 脚本（deploy/internal-test/scripts/）

  lib/common.sh                 公共库：日志/配置/**端口策略守卫**/安全组提醒
  00-install-system-deps.sh     系统依赖（含 MySQL bind-address=127.0.0.1）
  01-pull-code.sh               拉取 GitHub 最新代码（PAT/Deploy Key）
  02-build-cpp.sh               本机编译 jt_db → .cpython-*.so
  03-setup-python-env.sh        venv + 依赖（清华镜像）
  04-configure-env.sh           生成 server.env（随机 JWT / 随机访问令牌）
  05-init-database.sh           导入 45 表 + 种子（密码经临时 my.cnf，不进 ps）
  06-install-systemd.sh         安装 systemd（8080），含渲染后端口二次校验
  07-firewall.sh                放行 8080；**显式拒绝 80/443/3306/6379**
  08-deploy.sh                  ★一键：拉码 → 装依赖 → 编译 → 配环境 → 重启 → 健康检查
  09-restart.sh                 仅重启 + 就绪检查
  10-status.sh                  体检：服务/端口/闸门/DB/防火墙/日志
  99-uninstall.sh               卸载（保留代码与库）
  systemd/xiaojietong-api.service   单元模板（--port __APP_PORT__，workers=1）

端口策略守卫 assert_port_policy()：任何非 8080 的监听会**直接终止脚本**。

## 第二部分 · 部署文档（deploy/internal-test/docs/）

  README.md                   索引 + 快速开始 + 红线清单
  01-部署方案.md               架构/端口策略/目录/流程图/已知限制
  02-部署操作手册.md           安全组 + 防火墙 + 逐步命令 + 启动 + 访问 + 注意事项
  03-访问鉴权说明.md           闸门机制/配置/调用/令牌管理/边界
  04-ICP备案风险提示.md        ⚠️ 监管依据 + 12 条禁止 + 后果 + 7 项自查
  05-运维排障与验收清单.md     按症状排障 + 6 组验收清单 + 回滚

## 代码 · 访问闸门

  backend/app/core/access_gate.py   纯 ASGI 中间件（不用 BaseHTTPMiddleware，
                                    避免破坏 SSE 流式输出）
  backend/app/core/config.py        + access_token / access_gate_exempt
  backend/app/main.py               最后注册 → 位于最外层（拒陌生请求不耗限流配额）

要点：
- 令牌为空 → **完全不挂载中间件**，本机开发零回归
- 常量时间比较（hmac.compare_digest）
- 403 走统一契约 {code:2003,message,data}
- OPTIONS 预检放行
- ⚠️ 豁免改为**精确匹配**：最初的前缀匹配会连带放行
  /api/v1/health/detail（匿名 200，泄露 DB/连接池状态）—— 由验证工具发现

## 工具

  tools/verify_access_gate.py       闸门行为验证（启用态 12 项 / 关闭态 2 项）
  tools/check_deploy_constraints.py 约束合规校验（违禁模式 + 必备要素 + 端口纪律）

## 实测结果

- 访问闸门启用态：**12/12 通过**（含「敏感探针必须被拦」与「正确令牌须放行」）
- 关闭态：**2/2 通过**（零回归）
- 约束合规：**全部合规**（11 项违禁模式零命中，20 项必备要素齐备）
- 启动日志已明确输出「✅ 访问闸门已启用（豁免路径：/api/v1/health）」

## 已知限制（已写入文档）

1. 部署脚本**未做 bash -n 语法检查** —— 开发机为 Windows 且无 bash/WSL，
   首次在服务器执行若报语法错需即时修
2. 未在真实云服务器完整跑过一轮；故障速查表已覆盖常见问题
3. 明文 HTTP、单 worker、无自动备份/监控
4. XJT_ENV=dev（prod 会因无微信凭据拒绝启动），正式上线必须切换

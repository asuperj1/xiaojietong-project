# 校捷通 · 成员1（前端）任务单 · 2026-09-18

> **基线**：`github/dev` = **`4460113`**（2026-09-18 实测，含 `#110~#122` 全部合并）
> **范围**：本单只覆盖**前端** —— `miniprogram/`（可写）+ `ui/`（**只读**，素材归成员4/`D8`）
> **隶属**：本单是 `docs/成员任务单-二阶段整改260912.md`（**dev 上为 v3**）的**成员1 切片**；若两者冲突，**以总表为准**。
> **一句话现状**：**地基已恢复、F15/F16 已合 ⇒ 现在卡的不是能力，而是「已经做完的 `F12` 还没开 PR」。**

---

## 0. 🔴 今日第一件事：把 `F12` 收口（唯一「做完却没 PR」的交付）

**实测事实**（本单全部结论均已实测，命令见括号）：

| 项 | 实测值 |
|---|---|
| 分支 | `agent/f12-custom-tabbar-poc`，**2 个提交**（`b9d46e4` POC + `6e2f285` 收尾） |
| 改动量 | **22 文件 / +1611 −4**，含 `miniprogram/custom-tab-bar/`（4 文件）+ `utils/tabbar.js` + `utils/tab-order.js` |
| 自带校验 | `tools/verify_f12_tabbar_poc.js`（31 KB）+ `tools/negative_control_f12_tabbar_poc.js`（17 KB，**31 组变异**） |
| 落后 `dev` | **163 个提交**（`git rev-list --left-right --count github/dev...github/agent/f12-custom-tabbar-poc`） |
| **与 `dev` 冲突** | ❌ **`miniprogram/pages/service/service.js`**（`git merge-tree --write-tree --name-only github/dev github/agent/f12-custom-tabbar-poc` → **EXIT=1**） |
| 与其它在飞分支 | ✅ `feat/b19-ddl-pack` / `feat/b21-b22-home-search` / `feat/b31-takeaway` 三者**均 EXIT=0**（不互堵） |
| 未接入 `dev` | `miniprogram/app.json` 的 `tabBar` **无 `custom: true`**（实测：5 项 list，无 custom 字段） |

> 冲突原因明确：`F12` 从 `dev` 切出后，`F15` 服务页重构（`ba0b70e`）才合入 `dev`，
> 两边都改了 `service.js`/`service.wxss`（页面底部留白适配 TabBar）。**不是设计冲突，是解一次即可的机械冲突**。

### 步骤

```bash
git fetch github
git switch agent/f12-custom-tabbar-poc
git merge github/dev            # 解 miniprogram/pages/service/service.js（页面底部留白：保留 F15 结构 + F12 留白）
git status --short              # 确认只剩 miniprogram/（不得出现 backend/ db/ ai/）
```

**解冲突口径（先看再改，别整份取一侧）**：本次冲突是**两段互不相干的改动落在同一文件**，逐条保留即可：

| 侧 | 改了什么（实测） |
|---|---|
| `F12` | `service.js`：顶部加 `require('../../utils/tabbar')` + 新增 `onShow() { syncTabBar(this, 'service') }`；`service.wxss` 末尾加**页面底部留白**（`@@ -24,3 +24,9 @@`） |
| `F15` | 服务页整体重构（路线维保中心吸顶卡 + 8 宫格 `data.gridItems`） |

⇒ 两边**都要保留**（不得为了解冲突把 `F15` 结构退回旧版）。
⚠️ 不要用 `git checkout --theirs miniprogram/pages/service/service.js`（会丢掉同一文件里已自动合并的其它 hunk，本仓已踩过）。
**解完自检（本仓「解冲突质量四连」之一）**：`git diff github/dev <你的分支> -- miniprogram/pages/service/service.js` 应当**只有 `+`、零 `-`**。

### 解完必须跑两条（**双向**，缺一不可）

| # | 命令 | 期望 |
|---|---|---|
| 1 | `node tools/verify_f12_tabbar_poc.js` | 全部 `[OK]`，退出码 **0** |
| 2 | `node tools/negative_control_f12_tabbar_poc.js` | 基线 PASS **且 31 组变异逐个被抓**，退出码 **0** |

> 第 2 条是这个 POC 的价值所在：**阴性对照**证明第 1 条的断言不是「只会 PASS」。
> 解冲突动了 `service.js`/`service.wxss` ⇒ 必须重跑，不能凭「冲突解完了」就过。

### 然后开 PR（三段式链接，`?base=dev` 那种会被忽略）

```
https://github.com/asuperj1/xiaojietong-project/compare/dev...agent/f12-custom-tabbar-poc?expand=1
```

PR 描述里必须写清：**真机抽测清单**（代码断言覆盖不到的部分）——
凸起位置/布局、真实震动、弹窗观感、隐藏动画顺滑度、iOS 安全区。

---

## 1. 昨日核定（2026-09-16 ~ 09-17 已进 `dev`，逐条附证据）

| 任务 | 交付物（实测） | 证据 / 命令 |
|---|---|---|
| **前端地基恢复** | `#89` 回退波及的 24 项（`F10` 玻璃库 3 + `FRONT-01` 守卫 + `F11` 接入 12 svg + 工具 2） | `#109` → `ace9314`；`Test-Path miniprogram/utils/glass.js, miniprogram/styles/{tokens,glass}.wxss` |
| **`F10` 设计令牌 + 玻璃库** | `styles/tokens.wxss` · `styles/glass.wxss` · `utils/glass.js` | `node tools/verify_glass_probe.js` → **EXIT=0**（12/12） |
| **`F11` 苹果线性图标（部分）** | `miniprogram/static/icons/`：**12 svg** + `user-solid.svg` + README | `node tools/verify_f11_icon_set.js` → **[PASS] 29 项** · **EXIT=0** |
| **`F15` 服务页重构** | 路线维保中心吸顶卡 + 8 功能 2×4 玻璃宫格 | `ba0b70e`（`git log --oneline github/dev -- miniprogram/pages/service`） |
| **`F16` 论坛搜索 + 7 标签栏** | 搜索框 + 7 标签（复用 `?category=`）+ 独立评审收敛 | `fe145ff` / `eb85b53`，经 **`#122`**（`ef44707`）合入 |
| **`F16` 校验工具** | `tools/verify_f16_forum_search.js`（含 CRLF 读取侧归一化） | `80090c9`；`node tools/verify_f16_forum_search.js` → **EXIT=0** |
| **`FRONT-01`（T-03~T-05）** | `services/request.js` 双入口 try/catch · `app.js` 启动自检 · `config/env.js` https 断言 | `node tools/verify_frontend_base_url_guard.js` → **[PASS] 26/26** · **EXIT=0** |

> ✅ **四个前端校验工具在 dev 树上全部 EXIT=0**（本单实测，Node v24.18.1，无需微信开发者工具）。

---

## 2. 今日必交（阻断级，3 条）

### 2.1 `F12` 开 PR（见 §0）
**为什么是阻断级**：TabBar 影响**所有页面**的底部留白与选中态；它一天不落地，
`F13`/`F14`/`F17`/`F18` 四页的布局都要做第二遍适配。**先把这条堵住再开新页面。**

### 2.2 `F11` 素材缺口闭环 + 修掉一个"污染源"

**实测两个坑（别被文件名骗）**：

| 项 | 实测 |
|---|---|
| `ui/icons/navigation/user-solid.svg` | **不存在**（该目录 13 个 svg，`user.svg`/`user-group.svg` 有，`user-solid.svg` 没有） |
| `miniprogram/static/icons/user-solid.svg` | **存在，但内容是描边版**（历史误命名）⇒ 它不是实心图标，不能当「已补齐」 |

**今日交付**：
1. 出**素材清单**交成员4（`D8`）：文件名 `user-solid.svg`、尺寸与 `ui/icons/navigation/*-solid.svg` 一致、
   `fill="currentColor"` 无 `stroke`、留白对齐现有 `user-group-solid.svg`（**只交清单，绘制归 `D8`**）；
2. 在 `D8` 交付前，先**不要让误命名的 `static/icons/user-solid.svg` 混进替换流程** ——
   要么就地替换成真·实心版，要么在 `static/icons/README.md` 里标注「此文件为误命名，待替换」；
3. 顺带：`F11` 的**全站替换**只做到「接入」这一步，`pages/*` 里仍在用旧 `.png` 图标的地方要列清单
   （实测 `miniprogram/static/icons/` 下仍有 **10 个 `.png`**：`ai-assistant/food/forum/free-room/job/library/map/notice/secondhand/task`）。

### 2.3 `F22` 小程序静态检查 —— 规则本体（无依赖、可离线、当天可交）

**实测现状**：`tools/` 下**没有任何通用小程序自检脚本**（只有 4 个页面级 `verify_*.js`）；
`.github/workflows/ci.yml` **无 `node`/`miniprogram` 步骤**（`git grep node github/dev -- .github/workflows/ci.yml` 无命中）⇒ 前端不在 CI 覆盖内。

**今日落地 `tools/verify_miniprogram_static_rules.js`**，规则取自本仓**已实际踩过**的坑类：

> 📌 **文件名**：本单最初写的是 `tools/verify_miniprogram_rules.js`；实际落地（PR **#125**）为
> **`tools/verify_miniprogram_static_rules.js`**（作者已在 README 与 PR 里标注“CI 用实际路径、不另加 alias”）。
> 本单已同步为实际文件名，下文命令一律用实际路径。

| # | 规则 | 为什么（本仓实证） |
|---|---|---|
| 1 | 分类/标签类点击不得用 `e.detail.value`（应为 `e.currentTarget.dataset.index`） | 一阶段 P0：5 个页面点击无反应 |
| 2 | 列表页必须有 `onReachBottom`（或用别的分页触发） | `life/index`+`life/notices` 曾缺 |
| 3 | 时间显示不得 `new Date('Y-m-d H:i:s')`（iOS → Invalid Date） | 本仓改用纯字符串截断 |
| 4 | 列表渲染必须兜底 `(res && res.items) \|\| []` | 后端字段缺失时页面白屏 |
| 5 | 不得再出现写死的 `BASE_URL`（须走 `config/env.js#getBaseUrl`） | `FRONT-01` 的契约 |
| 6 | 新增页面须在 `app.json` 注册；tabBar 项与 `pages` 一致 | 漏注册 = 跳转失败 |

**规格要求（否则等于没做）**：
- **必须有反向对照**：脚本内自带 `--self-test`，把上表每条规则的**已知缺陷样本**注入临时副本，
  要求**每条都被对应断言抓到**（照抄 `tools/negative_control_f12_tabbar_poc.js` 的做法）；
- 退出码：`0` = 全过；`1` = 有失败；
- 在 README 写明「**本脚本覆盖不到什么**」（视觉呈现、真机性能、`@supports` 类语法 —— 后者不在 WXSS 官方列举内，必须真机抽测）。

> ⚠️ **接 CI 是跨成员动作**：`ci.yml` 属 `B34`（成员2）。今天先把脚本与自证做扎实，
> 再接 `B34`（一句话需求：`runs-on` 加一个 node job 跑 `node tools/verify_miniprogram_rules.js`）。

---

## 3. 今日应做（无阻塞，按「解锁多、依赖少」排序）

| 顺序 | 任务 | 前置（**2026-09-18 实测**） | 今日目标 |
|---|---|---|---|
| 1 | **`F14` AI 助手侧边栏**（需求 6） | ✅ **前置全就绪**：`B24`（`POST /chat/conversations`，#99 已合）· `B32`（语音，#115 已合） | 左滑侧栏 + 历史列表 + 新建会话可对话；实测 `pages/chat` 无 `sidebar`/`voice` 标记（`history`/`新建` 已有） |
| 2 | **`F18` 校园地图容器**（需求 10） | ✅ 无后端依赖（`B13` 导航一阶段已完成） | 大容器（≥60% 屏）+ 顶部导航；实测 `pages/map/index.wxss` 仅 **1183 B**（一阶段版） |
| 3 | **`F13` 首页（先静态）**（需求 1~4） | ⛔ 数据依赖 `B21`/`B22` **不在 `dev`**（`backend/app/routers/home.py`、`search.py` 实测缺失，代码在 `feat/b19-ddl-pack`） | 只做**布局与交互**：轮播（`swiper` 实测 0 命中）、搜索上拉吸顶、历史增删、信息流骨架 |
| 4 | **`F17` 我的 + 设置页（先骨架）**（需求 9） | ⛔ `B23` 未做（`C22` 今日已合 ⇒ 现在可做，属成员2）；`pages/user` 实测只有 `user.*`+`favorites.*`，`student_no` 仅出现在 `user.wxml` | 设置页骨架（上传头像/改昵称/绑学号）+ **两类错误提示**（已被占用 / 修改过于频繁）文案先定 |

---

## 4. 阻塞与协调（不是你的锅，但今天要挂在嘴上）

| 依赖 | 实测现状 | 卡的谁 | 建议动作 |
|---|---|---|---|
| `B21`/`B22`（首页/搜索接口） | `backend/app/routers/{home,search}.py` **不在 `dev`**（在 `feat/b19-ddl-pack`，PR **#82 已关未合**） | **`F13` 数据部分** | 找成员2 确认重开 PR 的时间；`F13` 先做静态 |
| `B31`（代收闭环） | `feat/b31-takeaway` **3 提交未合**（`aafdb62`/`5506c7d`/`9eb01a7`） | `F19` 代收订单详情 | 同上；先做「全站移除代买入口」（实测 `代买` 在 `miniprogram/` **已 0 命中** ⇒ 这条已天然满足） |
| `B23`（学号绑定接口） | 未做（`C22` 今日已合入 `dev`，前置解除） | `F17` 绑学号 | 提醒成员2 排期 |
| `D8` 素材 | `ui/icons/navigation/user-solid.svg` **缺** | `F11` 全站替换 | 今日出清单（§2.2） |
| `D11` 驿站素材/样式参考 | **0%** | `F21` | 先搭结构，等素材 |
| `B34` CI | `ci.yml` **无 node 步骤** | `F22` 纳入 CI | 脚本就绪后一句话提需求 |
| `B33`（`C33` 抽取结果展示，`F20` 可选） | `C33` 已合入 `dev`（`#119`） | `F20` | 可选，最后做 |

---

## 5. 收工验收清单（**逐条打勾**，别只看「没报错」）

| # | 检查项 | 命令 | 期望 |
|---|---|---|---|
| 1 | `F12` 解冲突后只剩前端文件 | `git status --short` | 只出现 `miniprogram/`；**不得**出现 `backend/` `db/` `ai/` |
| 2 | `F12` 代码级校验 | `node tools/verify_f12_tabbar_poc.js` | 退出码 **0** |
| 3 | `F12` 断言有效性 | `node tools/negative_control_f12_tabbar_poc.js` | 退出码 **0**（31 变异全被抓） |
| 4 | `F12` 与 `dev` 可干净合并 | `git merge-tree --write-tree --name-only github/dev <你的分支>` | **EXIT=0**（只输出 tree sha） |
| 5 | 老地基没被碰坏 | `node tools/verify_glass_probe.js` `verify_frontend_base_url_guard.js` `verify_f11_icon_set.js` `verify_f16_forum_search.js` | 四条 **EXIT=0** |
| 6 | 接入自定义 TabBar 且顺序单一来源 | 打开 `miniprogram/app.json` + `miniprogram/utils/tab-order.js` | `tabBar.custom = true`，且 `list` 的 5 项与 `utils/tab-order.js#TAB_LIST` **逐项一致**（该文件是 JS 侧唯一事实来源，由校验脚本断言） |
| 7 | `F22` 规则脚本自证 | `node tools/verify_miniprogram_static_rules.js --self-test` | 退出码 **0**（每条规则的注入样本都被抓） |
| 8 | 今日新页面在 `app.json` 注册 | `git grep -n "\"pages/" github/dev -- miniprogram/app.json` | 新增页面均已注册 |

> ⚠️ **反向对照不能省**（本仓已踩过两次）：「现在通过」只证明现在好 ——
> 涉及断言的新脚本/新用例，**先跑一次预期失败的输入**（缺陷样本 / 修复前实现），
> 两边都跑过才算数。

---

## 6. 冲突实测矩阵（2026-09-18 实测，`git merge-tree --write-tree --name-only`）

| × | `dev` | `feat/b19-ddl-pack` | `feat/b21-b22-home-search` | `feat/b31-takeaway` |
|---|---|---|---|---|
| **`agent/f12-custom-tabbar-poc`** | ❌ **`miniprogram/pages/service/service.js`** | ✅ EXIT=0 | ✅ EXIT=0 | ✅ EXIT=0 |

> 今日若能合 `F12`，请**顺手重跑这张表** —— 前端改动只跟前端分支互堵。

**合并门禁**：① 分支落后 `dev` 必须为 **0**（先 `git merge github/dev`）；
② 上述校验脚本全绿；③ 队友 Approve（**本人不能自批**）；④ **批准后立刻合并**
（往 head 再推 或 `dev` 前移都会让批准作废）。

---

## 7. 环境与命令速查

| 事 | 命令 |
|---|---|
| 跑前端校验（**无需微信开发者工具**） | `node tools/verify_*.js`（本机 Node v24.18.1）；`negative_control_*.js` 是配套的阴性对照 |
| 看某接口真实返回字段 | `E:/miniconda3/python.exe -X utf8 tools/pr39_field_probe.py` |
| 探路由/方法是否存在 | `E:/miniconda3/python.exe -X utf8 tools/pr39_method_probe.py` |
| 起后端联调 | `backend/` 下 `uvicorn app.main:app --port 8000`；健康检查真实路径是 `/api/v1/health` |
| 真机调试联调地址 | 开发者工具写 storage `xjt_api_base_url`（**预览/真机调试 = `develop` 环境，允许覆盖**） |
| 冲突无副作用预检 | `git merge-tree --write-tree --name-only github/dev <分支>`（`EXIT=0` + 只输出 tree sha = 无冲突） |
| ⚠️ 网络 | 本机系统代理 `127.0.0.1:7897` **今天没在监听** ⇒ `git fetch` 报 `Failed to connect … over proxy` 时，用 `git -c "http.https://github.com.proxy=" -c http.proxy= -c https.proxy= fetch github` 直连 |

---

## 8. 本单与总表的对应（备忘）

- 编号体系 = **v3（2026-09-14 全员重排）**，见 `docs/成员任务单-二阶段整改260912.md` §3.3（`F10~F22`）。
- 本单**不改总表**；若今日进度与总表状态不符，**以总表为准**，并在总表里更新状态（状态列：`✅ / 进行中 / 未做`）。
- 素材分工仍按 09-14 定稿：`F11`/`F12`/`F13`/`F21` 的**图像绘制与素材**归成员4（`D8`/`D11`），
  成员1 只出**素材清单** + 做接入与样式填充。

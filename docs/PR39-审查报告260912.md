# PR #39 审查报告

> **PR**：[#39 feat(frontend): complete F1-F9 MVP and integration fixes](https://github.com/asuperj1/xiaojietong-project/pull/39)
> **流向**：`feature/frontend` → `dev`　｜　**head**：`ca9db97`　｜　**提交数**：9
> **规模**：131 文件、+5902 / -0（全部集中在 `miniprogram/`）
> **作者**：`dream-facing`（成员1）　｜　**审查人**：成员3　｜　**审查日期**：2026-09-12
> **审查方式**：GitHub API 元数据 + 本地完整检出（`xjt-frontend @ ca9db97`）静态审查 + 编译阻断项自动检查
> **审查时状态**：`mergeable = clean`（无冲突）　｜　**尚无任何 review / 评论 / 指定 reviewer**
>
> ⚠️ **历史快照**：本文记录 **2026-09-12 当时**的审查结论（该 PR 后续已合入 `dev`），**不作为当前封测操作依据**。

---

## 一、审查结论

> **建议 Approve（Squash and merge）** —— 分支归属合规、无冲突、无编译阻断、零调试残留；
> 3 条改进意见中仅第 1 条建议**合并前**处理，其余可作为后续迭代。
> **补充**：第二轮**深度审查**（字段级 + 方法级契约实测，见 **§八**）结论为
> **字段/方法级契约全部通过（19/20，唯一 404 为已知的后端缺失接口）**，仅增补 2 条 P2 建议。
> **合并前提醒**：`BASE_URL` 仍写死本机 http，属 **上线阻断项**（不阻断合入 `dev`，但阻断 `dev → main`）。

---

## 二、通过项（8 项自动检查全部通过）

| # | 检查项 | 结果 | 证据 |
|---|---|---|---|
| 1 | **分支归属规范** | ✅ | `git diff --name-status dev...ca9db97` 过滤后 **非 `miniprogram/` 改动为 0**（未越界改 `backend/`、`db/`、`docs/`） |
| 2 | **提交信息规范** | ✅ | 9 个提交全部符合 Conventional Commits：`feat(miniprogram):` ×5、`fix(miniprogram):` ×1、`Merge ...` ×3 |
| 3 | **与 dev 的同步** | ✅ | 3 次 `merge origin/dev`（`549fbe5`/`534be12`/`ca9db97`），`mergeable=clean` |
| 4 | **编译阻断·文件完整性** | ✅ | 29 个页面的 `js/json/wxml/wxss` **四件套齐全**（脚本校验，0 缺失） |
| 5 | **编译阻断·JS 语法** | ✅ | 全部 JS 通过 `node --check`（node v24.18.1，**错误文件数 0**） |
| 6 | **调试残留** | ✅ | 全仓 **无 `console.log` / `debugger` / `TODO`** |
| 7 | **静态资源有效性** | ✅ | 新增 10 个图标（2.8~4.0 KB，非空文件），且**全部被 `pages/index/index.js`、`pages/service/service.js` 引用**（非无效变更） |
| 8 | **PR 描述真实性** | ✅ | 描述中「注意事项」6 项与代码实际状态**逐条一致**，未夸大完成度 |

---

## 三、代码亮点（值得团队沿用）

1. **类型归一化系统化**（对应本报告 `CAC-16` 的数据层类型抹平问题，前端主动做了防御）
   ```js
   // pages/library/seat.js:50-58
   // 后端字段可能为字符串（如 id="1"、reserved="0"）：统一归一化。
   const numId = Number(s.id)
   reserved: Number(s.reserved) === 1,
   is_window: Number(s.is_window) === 1,
   ```
   同类处理遍布 `freeRoom.js:50`（`has_power`）、`secondhand/mine.js:28`（`Number(it.status) === 0`）、
   `agent/index.js:40-44`、`job/mine.js:24`、`library/myReserve.js:23`、`map/building.js:24`、`user/favorites.js:27`。
   → **这是对根因的正确识别**：C++ 层把查询值统一转 `str`，前端不再假设类型。

2. **点赞防重复（真实交互 bug 的正面修复）**
   ```js
   // pages/forum/detail.js:42-56
   // 后端为 toggle 语义：请求期间忽略后续点击，避免连点两次把状态翻转回原点
   if (this.data.liking) return
   ...
   // 不做前端乐观更新，一律以服务端返回的 liked / like_count 为准
   ```
   并配 `act--busy` 视觉反馈（`detail.wxml:22`、`detail.wxss:40`）。

3. **TabBar 跳转语义正确**
   `index.js:71-75`、`service.js:19` 按条目 `tab` 字段区分 `switchTab` / `navigateTo`；
   `history.js:51-52` 正确指出「`switchTab` 不能带参数，故经 `globalData` 传递」；
   `login.js:38-39` 登录后 `switchTab` 回首页。

4. **登录态失效处理完整**（对应审查报告 `SEC-22`，**已闭环**）
   ```js
   // services/request.js:16-33
   const AUTH_FAILURE_CODES = [2001, 2002, 2003]
   function authFailureMessage(code, message) {
     if (code === 2003) { ... return '账号已被禁用，请联系管理员' }
   }
   ```
   `handleAuthFailure` 同时清理 storage 与 `globalData`（内存态/存储态一致），普通请求与 SSE 共用同一处理。

---

## 四、需要修改的意见（3 条）

### ① 【建议合并前处理】`project.config.json` 未入库
- **现象**：`git ls-tree -r ca9db97 -- miniprogram/` 中**没有** `project.config.json` / `sitemap.json`；
  该文件仅存在于成员1 本地（`git status` 显示未跟踪）。
- **影响**：其他成员 `clone` 后**无法用微信开发者工具直接导入项目**（缺 `appid`、`miniprogramRoot`、`setting` 等配置），
  每人需重新选择目录与 AppID，易产生配置漂移。这正是 `docs/技术方向待处理问题.md` 的 **`FEAT-09 ⑤`**。
- **建议**：
  1. 提交 `miniprogram/project.config.json`；
  2. 把 `miniprogram/project.private.config.json` 写入 `.gitignore`（含个人调试偏好，不应共享）。

### ② 【建议本次顺手补】tabBar 图标已备资源但未配置
- **现象**：新增 10 个图标（含 `library.png`、`forum.png`、`ai-assistant.png` 等），但 `app.json` 的 `tabBar.list`
  仍是 `{ pagePath, text }` 结构，**无 `iconPath` / `selectedIconPath`** → TabBar 依旧只有文字
  （即 `FEAT-06 ③`「TabBar 无图标」未闭环）。
- **建议**：为 5 个 tab 配上图标（资源已在仓内，属几行配置）。
- ⚠️ **注意**：tabBar 图标规范为 **81×81 px**；现有图标是给首页宫格（较大尺寸）使用的，配置前请确认尺寸/无透明边问题。

### ③ 【可延后】空 `catch` 仍有残留
- **现象**：`pages/forum/detail.js:70` 收藏失败 `.catch(() => {})`；
  `pages/library/myReserve.js:44` 等处同类写法 —— 写操作失败**界面无任何反馈**，用户误以为成功。
- **说明**：`pages/library/seat.js:103-113` 的 `3001` 冲突处理已按规格修好（弹窗 + 清选择 + 重拉座位），
  属 **部分修复**；建议统一在 `request.js` 的 reject 上做兜底提示，避免页面各自遗漏。

---

## 五、上线前必办（不阻断合并 `dev`，但阻断 `dev → main`）

| 项 | 位置 | 说明 |
|---|---|---|
| **`BASE_URL` 写死本机 http** | `services/request.js:8`（`:3` 注释同样写死） | 小程序正式版**必须 https + 后台已备案合法域名**；`urlCheck: false` 也需在发布前恢复。PR 描述已将其列为待办，属诚实标注（对应 `FRONT-01`，P0） |

---

## 六、遗留清单核对（与 2026-09-12 整体审计报告对照）

| PR 描述中的「注意事项」 | 对应审计编号 | 当前状态 |
|---|---|---|
| refresh token 自动续期 | `FRONT-03`（P1） | ❌ 未做（全仓仍无 `/auth/refresh` 调用） |
| BASE_URL 多环境配置 | `FRONT-01`（**P0**） | ❌ 未做 |
| 发帖审核状态 | `FRONT-12`（P1） | ❌ 未做（无 `audit-status` 轮询） |
| 二手图片上传及正式详情接口 | `FRONT-05` + `DATA-10`（P1） | ❌ 未做 |
| 图书馆正式 rooms 接口 | `FRONT-07` / `FEAT-06 ⑨`（P1） | ❌ 未做（仍借用 `/map/building/1`） |
| 已同步的后端审计问题 | 后端 38 项 | ❌ 后端 `dev` 未有任何变化 |

> **结论：PR 描述与实际完全一致，无夸大、无隐瞒** —— 建议团队沿用这种「已做/未做」双清单的写法。

**本 PR 已闭环的审计条目**：`FRONT-02`（P0，5 个页面分类 tab 事件参数错误）、`SEC-22`（P1，2003 未处理）、
`FRONT-06`（P1，停止生成 + 历史会话）、`FRONT-04`（P1，**部分**：座位 3001 已修）。

---

## 七、合并操作建议

1. **合并方式**：`Squash and merge`（与 PR 描述一致，符合 `docs/团队Git合作协议.md`）。
2. **合并前**：处理意见 ①（提交 `project.config.json` 并 ignore `project.private.config.json`）。
3. **合并后**：由成员2/3 在 `dev` 上跑一次连通性验证
   （`python tools/e2e_connectivity.py`、`python tools/audit_frontend_backend.py`），
   确认前端 47+ 调用点在 `dev` 上仍全部可达。
4. **建议指定 reviewer**：该 PR 目前未指定审查人，建议在 GitHub 上指定成员2 或成员3，避免漏审。

---

## 八、第二轮深度审查（字段级 / 方法级契约实测）

> **方法**：不依赖静态推断，直接用已启动的后端（`dev@7a3260c`）**真实调用** PR head 实际使用的接口，
> 核对「后端真实返回的字段」与「前端 `pages/**/*.js` 读取的字段」、「前端使用的方法」与「后端注册的方法」。
> **复现**：`python tools/pr39_field_probe.py`（字段）｜`python tools/pr39_method_probe.py`（方法，只用不存在的 id/假 token，无副作用）

### 8.1 字段级契约：**全部一致** ✅

| 接口 | 后端真实返回 | 前端读取 | 结论 |
|---|---|---|---|
| `GET /library/rooms/1/seats` | `has_power, id, is_window, reserved, seat_no` | `seat.js` 读同名字段并做 `Number()` 归一化 | ✅ 一致 |
| `GET /library/free-rooms` | `building_name, capacity, floor, has_power, id, room_name` | `freeRoom.js` / `library/index.js` 读同名字段 | ✅ 一致 |
| `GET /map/building/1` | `address, campus, created_at, floor_count, floor_plan, id, latitude, longitude, name, services` | `building.wxml` 仅用 `name/campus/address/floor_count` + `floor_plan[].floor/capacity/has_power` | ✅ 一致（**未引用契约里缺失的 `hours`**，规避 undefined） |
| `GET /life/notices`、`/life/notice-feed` | `category, content, id, publish_time, source, title` | `notices.js` 用 `...n` + `publish_time` | ✅ 一致 |
| `GET /chat/conversations` | `items[{id, title, updated_at}]` | `history.js` 读 `c.id/c.title/c.updated_at` | ✅ 一致 |
| `agent_task.status` 语义 | `db/sql/03_agent.sql:13` = `0待执行 1执行中 2成功 3失败 4已取消` | `agent/index.js:5` `STATUS_TEXT` | ✅ **完全一致** |

### 8.2 方法级契约：**19/20 正确** ✅

| 结果 | 数量 | 说明 |
|---|---|---|
| ✅ 路由 + 方法均存在 | **19** | 包含全部高风险写接口：`PUT /agent/reminders/{id}/done`、`POST /agent/tasks/{id}/cancel`、`PUT /secondhand/items/{id}/status`、`POST /library/reservations/{id}/cancel`、`POST /life/notices/{id}/read` |
| ❌ 路径不存在 | **1** | `GET /secondhand/items/{id}` —— 后端确实未实现（= 审计 `DATA-10`，**已知**，非本 PR 引入） |

> 探测时未携带 token，因此返回 `401/2001`；这恰好证明**路由与方法都存在**（若路径错为 404、方法错为 405）。

### 8.3 前端防御性写法（亮点，建议团队沿用）

1. **列表映射全部带空值兜底**：`((res && res.items) || []).map(...)` → 后端返回空数据时**不会崩溃**（已核查 `history.js:26`、`notices.js:34`、`agent/index.js:36`、`building.js:19` 等）。
2. **`utils/format.js` 的时间格式化用纯字符串截取**（`String(v).replace('T',' ').slice(0,16)`），**不构造 `Date` 对象**：
   → 巧妙规避了 iOS 上 `new Date('2026-09-12 10:00:00')` 返回 `Invalid Date` 的经典跨端坑（**这是真机才能发现的隐患，前端主动避开了**）。
3. **全局态均形成写入+消费闭环**（逐一核查，非死代码）：
   `pendingSearch`（`index.js:67` 写 → `chat.js:39-46` 读后清空）、
   `chatRestore`（`history.js:47` 写 → `chat.js:33` 清空）、
   `secondhandNeedRefresh`（`publish.js:51` 写 → `secondhand/index.js:53-54` 消费并复位）。
4. **`library/index.js:2-3` 把技术债写进注释**（“后端暂无 `GET /library/rooms` 列表接口，图书馆建筑 id=1（种子数据「中心图书馆」），经真实地图建筑详情接口取阅览室楼层平面”）→ 债可见、可追溯，**优于静默硬编码**。

### 8.4 本轮新发现（2 条 P2 + 1 条复测建议）

#### [P2] ① `forumNeedRefresh` 确认为死代码
- **位置**：`pages/forum/create.js:42`（唯一写入点）；`pages/secondhand/publish.js:49` 的注释还写着“与 forumNeedRefresh 同一模式”。
- **证据**：全仓 grep `forumNeedRefresh` 仅命中**写入处**，**无任何读取点**（`forum.js` 依赖 `onShow` 全量刷新）。
- **影响**：误导后续维护（别人会以为这是有效范式，照拄到新页面）；也说明 `publish.js` 的注释需要更新。
- **建议**：删除 `create.js:42` 该行；或若确实需要“发布后立即刷新”，在 `forum.js` 的 `onShow` 中真正消费它。

#### [P2] ② 触底加载覆盖不全（比上轮有改进）
- **现状**：`forum/forum`、`job/index`、`secondhand/index` **已补** `onReachBottom`；
  但 **`life/index`、`life/notices` 仍只有 `onPullDownRefresh`**，`size: 20` 之后无法加载更多。
- **建议**：这两个页补 `onReachBottom`（可照拄已实现的 3 个页面，模式已成熟）。

#### [建议] ③ SSE 多分片需在 Ollama 就绪时复测
- **现象**：本次实测 `/chat/send` **只产出 1 个分片**（上轮为 26 片），因为 `/health/detail` 显示 `ollama.reachable=false`，正走降级路径（返回固定提示语）。
- **说明**：**属环境问题而非 PR 缺陷**；但 PR 描述声称“AI SSE / 历史会话 / 停止生成实际联调通过”，
  建议合并前在 `xjt-3b` 已加载的状态下**复测一次**，以证实多分片流式确实正常。

#### [参考] ④ 状态文案：两种做法共存，建议统一
- `agent/index.js:5` 的 `STATUS_TEXT` 与 DB 语义**完全一致**（正确范式）；
- 但 `life/order.js:5` 的订单状态仍与 `db/sql/09_life.sql` 错位（`FRONT-09`，**本 PR 未触及该文件**）。
- **建议**：以 `agent/index.js` 为范式，把订单状态映射一并校对。

### 8.5 深度审查小结

| 维度 | 结果 |
|---|---|
| 字段级契约（22 个接口） | ✅ **全部一致**（无“字段名不匹配 → 界面空白”风险） |
| 方法级契约（20 个接口） | ✅ **19/20 正确**（唯一 404 为已知的后端缺失接口） |
| 数据兜底 / 时间格式化 / 全局态闭环 | ✅ 均规范（发现并规避了 iOS Date 坑） |
| 新增建议 | 2 条 P2（`forumNeedRefresh` 死代码、触底加载覆盖）+ 1 条复测建议 |
| **对合并结论的影响** | **不改变** —— 仍建议 **Approve**（新增项均非阻断） |

---

## 九、评论往来与处理方案（2026-09-12 追补）

### 9.1 评论往来梳理

| 时间 | 作者 | 内容要点 |
|---|---|---|
| 08:19 | `asuperj1`（负责人） | 转达审查意见 ①②③（`project.config.json` 未入库 / tabBar 未配图标 / 空 catch 残留） |
| 08:26 | `dream-facing`（作者） | ① ②「这轮补掉」；③「记入后续统一处理」；修完继续推 `feature/frontend`，请再 review |
| 08:32 | `dream-facing` | **提出两个需确认的细节（阻塞中）**：<br>**A** 入库的共享 `project.config.json` 该用哪个 AppID —— 他本机 `wxdac97100002f8adf`，审计文档记录 `wx6ccc5c4c02b31455`；<br>**B** TabBar 图标是「基于 `ui/icons/navigation/` 的 SVG 转 PNG」，还是另有现成资源 |

> **PR head 未变**（仍 `ca9db97`），作者尚未推新提交；保持 `mergeable=clean`。

### 9.2 问题 A：AppID 统一用 `wx6ccc5c4c02b31455`

**事实核查（本轮实测）**

| 核查项 | 结果 |
|---|---|
| `xjt-frontend/miniprogram/project.config.json`（未跟踪）的 `appid` | **`wx6ccc5c4c02b31455`** |
| 与 `docs/前后端连接性与接口审查报告260912.md:203` 记录 | ✅ 一致 |
| `wxdac97100002f8adf` 在仓库中的出现 | **0 次**（`git log --all -S` 零命中 ＋ 全仓文本零命中）→ 纯属作者本机个人配置 |
| 后端是否配置微信凭据 | `backend/.env` 不存在、`.vscode/launch.json` 无 `XJT_WX_APPID` → 当前走 **mock 登录**，不校验 AppID 匹配 |

**结论与指示**

1. **统一使用 `wx6ccc5c4c02b31455`**：① 它出现在负责人的工作区配置中；② 已被审计文档固化为团队记录；③ 作者的值在仓库中零出现（个人测试号特征）。
2. **`appid` 属公开信息，入库无风险**；但 **AppSecret 绝不能入库**，只走后端 `XJT_WX_SECRET` 注入。
3. ✅ **负责人已于 2026-09-12 确认采用 `wx6ccc5c4c02b31455`** —— 该结论已成为**团队决定**，作者按此执行即可（无需再次确认）。
4. ⚠️ **`project.private.config.json` 不要入库**（作者「两个的仓库配置这轮补掉」的表述需澄清）：
   - ✅ 提交 `miniprogram/project.config.json`
   - ❌ 不提交 `miniprogram/project.private.config.json`（个人调试偏好，会与他人冲突）
   - ➕ `.gitignore` 追加规则（**实测当前 `.gitignore` 无任何 `project*.json` 规则**）：`miniprogram/project.private.config.json`

### 9.3 问题 B：**本 PR 不做 tabBar 图标**，建议另开小 PR

**事实核查（本轮实测）**

| 核查项 | 结果 |
|---|---|
| `ui/icons/navigation/` 现有 SVG | `home.svg`/`home-solid.svg`、`chat-bubble-left-right.svg`/`-solid.svg`、`squares-2x2.svg`/`squares-2x2-solid.svg`、`user-group.svg`/`user-group-solid.svg`、`user.svg` |
| ❌ **缺口** | **`user-solid.svg`（「我的」选中态）不存在** → 5 组只有 4 组完整 |
| `miniprogram/static/icons/*.png` 实际尺寸 | **192×192**（10 个，彩色插画风，供首页宫格/服务页）→ **不能当 tabBar 图标**（超规、彩色风格不符、无选中态） |
| tabBar 规范 | `iconPath`/`selectedIconPath` 需 **81×81 PNG**；**不支持 SVG、不支持网络图片、不支持 base64**；单文件 ≤ 40KB |
| 资源归属 | `ui/` 目录按 `docs/分支规划与文件归属.md` 归 **`feature/ui`** 分支，不属于 `feature/frontend` |

**建议（推荐方案）**：本 PR **不补** tabBar 图标，保持文字 tabBar（零功能影响）。理由：
1. 源文件缺 1 个（`user-solid.svg`），现在做会产生风格不一致；
2. SVG→PNG 属**设计资源交付工序**，塞进前端 PR 会造成 `ui/` 与 `miniprogram/` **跨模块耦合**（违反目录归属规范）；
3. 图标属视觉优化，与 `FRONT-01`（BASE_URL，真正的上线阻断项）优先级不可比。

**另开小 PR（`feat/ui-tabbar-icons`）的完整步骤**：
1. 补齐 `ui/icons/navigation/user-solid.svg`（按既有风格）；
2. 5 组 × 2 态 用同一套 SVG 导出 **81×81 PNG**（工具不限：`sharp-cli`、Inkscape、在线转换均可）；
3. PNG 落到 `miniprogram/static/tabbar/`（建议命名 `home.png` / `home-active.png` …）；
4. 前端只改 `miniprogram/app.json` 的 `tabBar.list` —— 每项补 `iconPath` 与 `selectedIconPath` 两个字段（共 10 行）。

> 若负责人坚持本轮完成，请作者先补 `user-solid.svg` 再按上述步骤执行，并在 PR 描述中说明「图标风格暂由 SVG 转换、待设计复核」。

### 9.4 意见 ③ 的自我修正（对作者更公平的结论）

复核 `services/request.js` 的实现：**所有业务错误与网络错误都已在 `request.js` 内统一 `wx.showToast` 提示**（错误分支 `showToast(message)`、网络分支 `showToast('网络异常…')`）。

因此 `.catch(() => {})` 的**真实影响是「页面侧未做错误后的状态恢复」，而非「用户无任何反馈」**：
- ✅ 用户**能看到 toast**（来自 `request.js`）；
- ⚠️ 剩余风险仅限：个别页面失败后可能残留 `loading`/`submitting`（经核查主要页面均在 `.catch` 内复位，如 `seat.js:100-113`）。

**结论**：意见 ③ 应从「P1 体验缺陷」**降级为「代码一致性建议」**（统一约定：`request.js` 兜底提示，页面只管状态恢复），**不要求本 PR 修改**。原审计报告 `FRONT-04` 的表述已同步修正。

### 9.5 可直接粘贴到 PR 的回复文本

> 📄 **最终版已单独成文并定稿**：[`docs/PR39-回复评论260912.md`](./PR39-回复评论260912.md)
> —— 已按负责人确认的 AppID 结论更新，并附「事实依据留档表」。**请使用该文件的内容回复 PR。**
> 以下为讨论期草案，保留以便对照：

```markdown
@dream-facing 感谢快速响应，两个问题都已核查，结论如下：

**A. AppID 统一用 `wx6ccc5c4c02b31455`**
- 依据：`xjt-frontend/miniprogram/project.config.json` 与《前后端连接性与接口审查报告260912》记录一致；
  而 `wxdac97100002f8adf` 在仓库历史与全仓文本中**零出现**，属你本机个人配置。
- 补充：`appid` 是公开信息，入库无风险；但 **AppSecret 绝不能入库**，只走后端 `XJT_WX_SECRET`。
- ⚠️ 一处更正：**`project.private.config.json` 不要入库**（含个人调试偏好，会与他人冲突）；
  只提交 `project.config.json`，并在 `.gitignore` 追加 `miniprogram/project.private.config.json`
  （当前 `.gitignore` 确实没有该规则，需补）。

**B. tabBar 图标这轮不做，建议另开小 PR**
- 实测：`ui/icons/navigation/` **缺 `user-solid.svg`**（5 组只有 4 组完整）；
  且 `miniprogram/static/icons/*.png` 是 **192×192 彩色插画**（宫格用），不符合 tabBar 的 81×81 规范。
- 另外 `ui/` 归 `feature/ui` 分支，SVG→PNG 属设计资源工序，塞进本 PR 会造成跨模块耦合。
- 建议：本 PR 保持文字 tabBar（零功能影响）；另开 `feat/ui-tabbar-icons`，由 ui 出 10 个 81×81 PNG，前端只改 `app.json` 10 行。
  若坚持本轮做完，请先补 `user-solid.svg`，再按审查报告 §9.3 步骤执行。

**C. 空 catch（意见③）降级**：复核后确认 `request.js` 已统一 toast，用户**不会**无反馈；
真实影响仅是页面侧状态恢复的一致性。**本 PR 不必改**，原结论已修正。

补完后请直接 push 到 `feature/frontend`（PR 会自动更新），我再 review 一次。
```

### 9.6 执行清单（谁 / 做什么 / 验收）

| # | 负责人 | 动作 | 涉及文件 | 验收 |
|---|---|---|---|---|
| 1 | 负责人（asuperj1） | 评论确认 AppID 取 `wx6ccc5c4c02b31455`（或反选并说明） | PR 评论 | 作者无歧义可执行 |
| 2 | 作者（dream-facing） | 提交 `miniprogram/project.config.json`（`appid` 改为统一值；`urlCheck` 建议同时改回 `true`） | `miniprogram/project.config.json` | 他人 clone 后可直接导入开发者工具 |
| 3 | 作者 | `.gitignore` 追加 `miniprogram/project.private.config.json`，并移出暂存区中的该文件 | `.gitignore` | `git status` 不再出现该文件 |
| 4 | 作者 | **不做** tabBar 图标（或按 §9.3 步骤做） | — / `app.json` | 本 PR 范围不扩大 |
| 5 | 负责人 + 作者 | tabBar 图标另开 `feat/ui-tabbar-icons`（ui 出 PNG → 前端改 `app.json`） | `ui/`、`miniprogram/static/tabbar/`、`app.json` | 5 组普通/选中态均正常 |
| 6 | 审查人 | 作者 push 后重跑 `tools/audit_recheck_260912.py` + `tools/pr39_method_probe.py` | — | 无新增回归 |

> 合并前仍建议确认 `BASE_URL`（`FRONT-01`，P0）已列入「上线前必办」——**不阻断合入 `dev`，但阻断上线**。

---
## 十、第三轮审查（新提交 `40e5248` 复核）

> **触发**：作者 08:49 回复「A 项已按确认结果修复并推送」。
> **方式**：`git show` 逐字段核对 ＋ **临时工作树中 `git check-ignore` 实测** ＋ 合并性检查（**未改动本地工作区**）。

### 10.1 变更范围（最小变更 ✅）

`40e5248 chore(miniprogram): add shared WeChat project config` —— 仅 **2 个文件**：
`.gitignore`（+3 行）、`miniprogram/project.config.json`（+25 行）。

### 10.2 逐项验收（对照 §9.2 提出的 A 项要求）

| 要求 | 结果 | 证据 |
|---|---|---|
| appid 统一为 `wx6ccc5c4c02b31455` | ✅ | 入库配置中 `appid` 字段值一致 |
| `urlCheck` 改回 `true` | ✅ | 入库配置中 `urlCheck: true` |
| `project.config.json` 入库 | ✅ | `git ls-tree` 可见 |
| `project.private.config.json` **不**入库 | ✅ | `git ls-tree -r --name-only github/feature/frontend -- miniprogram/` **仅**返回 `project.config.json` |
| `.gitignore` 追加规则 | ✅ | `.gitignore:47` = `miniprogram/project.private.config.json`（含中文注释） |
| **规则实测生效** | ✅ | 临时工作树中 `git check-ignore -v` 命中 `.gitignore:47`，**exit=0**；对 `project.config.json` exit=1（正常入库） |
| 无敏感信息 | ✅ | 全文无 `secret` / `appSecret` 字样 |
| **可干净合并** | ✅ | `git merge-base --is-ancestor github/dev github/feature/frontend` → **exit=0** |

### 10.3 新发现（P3 优化建议，不阻断合并）

入库版本是**新建的精简配置**，与开发者工具生成的完整版相比：

| 字段 | 本地完整版 | 入库版 | 影响 |
|---|---|---|---|
| `libVersion` | `3.5.5` | **缺失** | 基础库版本由工具默认决定 → **多成员/多环境可能不一致** |
| `projectname` | `xiaojietong` | **缺失** | 开发者工具项目列表显示目录名（轻微） |
| `setting.uploadWithSourceMap` | `true` | **缺失** | 上传体验版/正式版**不带 sourcemap** → 线上错误日志无法还原源码行号（有运维影响） |
| `setting` 键总数 | 35 | 9 | 其余由开发者工具在本地自动补全，**不影响编译** |
| `setting.minified` | `false` | `true` | 上传自动压缩 → **实际更好**（体积更小）✅ |

**建议**（可选，成本约 2 分钟）：补回 `libVersion: 3.5.5`、`projectname: xiaojietong`、`setting.uploadWithSourceMap: true` 三项。
> 若负责人希望尽快落库，**可直接合并**，这三项另开一个 tiny commit 补即可。

### 10.4 流程提示

`.gitignore` 是共享文件，本次在 `feature/frontend` 分支修改。内容与「忽略小程序个人配置」强相关，**可接受**；但若 `docs` 分支同期也在改 `.gitignore`，合并到 `dev` 时需注意冲突。

### 10.5 第三轮结论

> **建议 Approve（Squash and merge）** —— A 项要求 **7/7 全部落实且经实测验证**；
> 新发现 2 项均为 P3 优化建议（可选补丁），**不阻断合并**。
> 合并后建议在 `dev` 上重跑 `tools/e2e_connectivity.py`（24/24）与 `tools/audit_recheck_260912.py` 复核。

---
> **附：本次审查用到的命令（可复现）**
> ```powershell
> # 元数据（走本地 7897 代理）
> Invoke-RestMethod -Uri 'https://api.github.com/repos/asuperj1/xiaojietong-project/pulls/39' `
>   -Proxy 'http://127.0.0.1:7897' -Headers @{'User-Agent'='xjt-audit'}
> # 本地检出（已同步到 PR head）
> cd d:\xiaojietongproject\xjt-frontend
> git checkout feature/frontend && git merge --ff-only github/feature/frontend
> # 归属规范检查
> git diff --name-status github/dev...ca9db97 | Where-Object { $_ -notmatch 'miniprogram/' }
> # 编译阻断检查：页面四件套 + node --check
> ```
> ⚠️ 匿名 GitHub API 有 60 次/小时限流（经代理出口 IP 共享），批量查询请改用 `git` 命令或认证访问。

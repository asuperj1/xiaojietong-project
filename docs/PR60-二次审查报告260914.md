# PR #60 二次审查报告（2026-09-14）

> **PR**：`feature/backend` → `dev`（作者 `yl-love`，成员2）
> **head**：`d808a71`（上次审）→ **`eb7cccb`**（本次），新增 2 提交
> **增量**：12 文件 +536/−76
> **基线**：`github/dev = a0fd208`　｜　**落后 16 / 领先 4**　｜　`merge-tree` 冲突 **0**
> **完整评审结论**已发到 PR 评论：https://github.com/asuperj1/xiaojietong-project/pull/60

---

## 0. 结论

| 项 | 上次 | 本次 |
|---|---|---|
| **P0** `/life/notices` 私密行泄漏 | 🔴 未修 | ✅ **已修，且测试改造到位**（含反向对照） |
| **P0/P1** 两处恒真空断言 | 🔴 未修 | ✅ **已修**（三条联合验证） |
| **P1** `purge` LIKE 通配符未转义 | 🔴 未修 | ❌ **未修，且引入新回归（接口 500）** |
| **P1（新）** 自测未跑：3 个新用例红 | — | ❌ `pytest tests` = **3 failed / 69 passed** |
| `hdr_admin` 夹具 | 🔴 缺（2 个 403 失败） | ✅ 已补（原 2 项已转绿） |
| **B19** `14_notice_extend.sql`（新增） | — | ✅ 质量高，**守卫模式已用临时表实测** |
| **B20** 字段契约（新增） | — | 🟡 向后兼容做对了；`total` 仍是本页条数 |
| P2-2 上传先缓冲 / P2-3 PDF 解压上限 / P3×2 | 🟡 | ⏳ 本轮未涉及 → **仍开放** |

**结论：Request changes** —— P0 修得很好，但 **P1 未修且把接口改坏**，加上 3 个新测试红 ⇒ 不能合。

---

## 1. P0 ✅ 已修（值得肯定）

`routers/life.py` 改为：

```python
private_ids = notice_scheduler.private_notice_ids()      # ① 先取私密行 id 集合
items = []
probe = page
for _ in range(3):                                       # ② 最多向后补拉 2 页
    rows = cpp_bridge.life_dao().page_notices(probe, size, category, target_grade)
    if not rows: break
    items.extend(r for r in rows if int(r["id"]) not in private_ids)
    if len(items) >= size or len(rows) < size: break
    probe += 1
items = items[:size]
```

- **方向正确**：`id` **确实**在 `LifeDAO.page_notices` 的 SELECT 里（上次的问题是判据字段 `target_grade` 拿不到）⇒ 过滤真的生效；
- **补拉 2 页**缓解了「过滤后本页变少 / 公共行不可达」。

### 测试改造（这才是关键）—— 三条联合验证 + **反向对照**

`tests/test_notice_push.py` 新增 `_insert_public_control_notice()`：
1. 私密行**确实存在**（查库 `WHERE id = ? AND target_grade LIKE '__push:%'`）；
2. 它的 id **不在** `/life/notices`；
3. **反向对照**：临时插一条**公共**通知，断言它**在**列表里 —— 证明 ② 的"不在"不是"接口返回空"造成的假通过。

> 这正是上次评审要求的「断言必须依赖一个**有缺陷时必然为真**的可观测事实」。
> 文件头还把"为什么旧断言是恒真空的"写进 docstring，防止后人改回去。**做得很好。**

---

## 2. ❌ P1 未修 + 引入 500 回归（阻断）

### 事实

调用方已改为传 `dry_run=`：

```python
# routers/admin.py
result = knowledge.purge_by_source_prefix(
    body.source_prefix, purge_vectors=body.purge_vectors, dry_run=body.dry_run)   # ← dry_run
except ValueError as exc:            # ← 只接 ValueError
    raise err_param(str(exc)) from exc

# cli/kb_import.py 同理，并读取 result['pattern'] / result['doc_ids']
```

但 **`backend/app/services/knowledge.py` 本 PR 一个字都没改**：

```python
def purge_by_source_prefix(prefix: str, *, purge_vectors: bool = True) -> dict:   # ← 没有 dry_run
    p = normalize_source(prefix)
    rows = cpp_bridge.query(
        "SELECT id FROM knowledge_doc WHERE source_url LIKE ? AND status != 2", [f"{p}%"])  # ← % 仍未转义
```

⇒ **`TypeError: unexpected keyword argument 'dry_run'`**，而调用方只 `except ValueError`
⇒ **`POST /admin/knowledge/purge` 与 CLI `--purge-source-prefix` 都会 500**。

### 同时，P1 的**原始危险仍然存在**

- `%` / `_` **仍未转义** ⇒ 传 `source_prefix="%"` 依然 `LIKE '%%'` **清空全库**；
- 无最小长度、无强制 `dry_run` 默认、无确认；
- 测试里引用的 `MIN_PURGE_PREFIX_CHARS` / `like_prefix_pattern` / `result['pattern']`
  **在仓库里完全不存在**（`git grep` 只命中测试文件自身）。

### 实测（作者自己新加的 3 个用例，全部失败）

```
FAILED tests/test_kb_import.py::test_like_prefix_escapes_wildcards
    ImportError: cannot import name 'MIN_PURGE_PREFIX_CHARS' from 'app.services.knowledge'
FAILED tests/test_kb_import.py::test_purge_prefix_guard_rejects_short_prefix
    Failed: DID NOT RAISE ValueError           # "" / "/" / "%" / "a" / "ab" 全部放行
FAILED tests/test_kb_import.py::test_purge_matches_literally_not_as_wildcard
    TypeError: purge_by_source_prefix() got an unexpected keyword argument 'dry_run'
```

> ⚠️ **三条测试恰好把三个缺口各证了一遍** —— 转义没做（ImportError）、
> 短前缀没拦（DID NOT RAISE）、`dry_run` 没实现（TypeError）。
> ⇒ **描述里的自测数字不可信，且 CI 必红**。

### 建议修法（照现有测试的预期实现即可）

在 `services/knowledge.py` 落地三件事，调用方就无需再改：
1. `MIN_PURGE_PREFIX_CHARS = 3`，`len(p) < MIN` → `raise ValueError`；
2. `like_prefix_pattern(p)`：转义 `\` `%` `_` 后再拼 `%`（或用 `LOCATE(?, source_url) = 1`）；
3. `purge_by_source_prefix(prefix, *, purge_vectors=True, dry_run=False)`，
   返回体加 `pattern`；`dry_run=True` 只返回 `matched`/`doc_ids` 不删除。

---

## 3. ✅ B19 `db/sql/14_notice_extend.sql`（新增，质量高）

- 幂等：`information_schema` 判定 + `PREPARE` 动态执行（与仓库既有模式一致）；
- 3 列（`deadline`/`materials`/`importance`）**全部 NULL 可空** + `AFTER` 定位 ⇒ 不破坏既有数据；
- 额外建 `idx_deadline`（B18/B23/B24 会按它查）；
- 自带自检 SELECT 与**回滚脚本**；文件头写明与 B18 的 `information_schema` 探测关系。

### 实测（**未动共享库的 `campus_notice`**：用临时表 `CREATE TABLE _b19_probe LIKE campus_notice` 复刻同一守卫模式）

```
第一遍后 列=deadline,materials,importance      ✅
第一遍后 idx_deadline 存在=1                    ✅
[skip] 第二遍 deadline 已存在                   ✅ 幂等
[skip] 第二遍 idx_deadline 已存在               ✅
第二遍后 idx_deadline 数量(应=1)=1              ✅ 未重复建
probe 临时表已清理                              ✅
```

⇒ **B19 可以放心作为 W1 的关键路径产出**（真正执行请由成员2 在 W1 统一做）。

---

## 附 · B20 交付核对（2026-09-14 补测，已同步发到 PR #60 评论）

> **B20 = 通知表结构扩展**（任务单 L283）：`campus_notice` 加 `deadline`/`materials`/`importance`（可空），波次 **W1**。
> 判定口径：**「完成 = 进 dev 且能跑通」** ⇒ 结论 = **未交付**。

### 已做好的部分 ✅（别返工）

| 产出 | 位置 | 评价 |
|---|---|---|
| DDL 脚本 | `db/sql/14_notice_extend.sql`（99 行） | 质量高；影子表实测幂等通过 |
| 列存在性探测 + 健康位 | `notice_scheduler.py:211 notice_extended_columns()` / `notice_deadline_supported()` | 思路正确，**且暴露了可观测健康位** |
| 列拼装 / 补查 | `notice.py:33 notice_columns()` / `:45 attach_extended_fields()` | 「列不存在就不 SELECT」= 向后兼容做对 |
| 接口接入 | `life.py:130` + `notice-feed` + `notices/unread` | 已接 |
| 接口文档登记 | `docs/api.md` v1.18（L797）+ `health` 字段（L598） | **做了**，写明与 v1.17 行为一致 |

### 还差的 5 项 ⚠️

| # | 缺什么 | 实测证据 | 影响 |
|---|---|---|---|
| ① | **DDL 未执行** | `campus_notice` 三列**一个都没有**；`idx_deadline` = **0** | `notice_deadline_supported()` 恒 `false` ⇒ B18 的「带截止时间通知」推送源**实际关闭** |
| ② | **Pydantic 契约缺失** | `git grep deadline -- backend/app/schemas backend/app/models` → **0 命中** | 任务单明确要求「定字段契约 + 写 DAO/Pydantic」 |
| ③ | **DAO 未改** | `LifeDAO.page_notices` SELECT 仍编译期写死旧 6 列 | 只能靠 `life.py:130` Python 侧按 id 补查绕过 |
| ④ | **B19 只完成 1/5** | 仅 `14_notice_extend.sql`；`15_home_banner` / `16_search_history` / `17_user_student_no` / `18_takeaway_pickup` **全缺** | 卡 `B21`/`B22`/`B23`/`C22`/`C25` |
| ⑤ | **位置不对（越界）** | B20 的 DDL 混在 B16/B17/B18 的 PR #60 里 | 违反任务单 L267「同批复审、同批提交」+「一事一议」；B19 因此永远凑不齐 |

> ⚠️ `17_user_student_no.sql` 的坑：**必须先** `UPDATE user SET student_no=NULL WHERE student_no=''`（实测 **63 行空串**），否则唯一索引 `ERROR 1062`。

### 补齐顺序（前 3 项当天可做）

1. 补 **Pydantic 模型**（不依赖 DDL）；
2. 把 `14_notice_extend.sql` **从 #60 抽出**，与 `15~18` 并入 **B19 的 DDL 打包 PR**（`?base=dev`）；
3. `notice_extended_columns()` 探测不到列时补 **`logger.warning`**（别只靠 health）；
4. **执行 DDL 并留证**：连跑两遍贴输出（第二遍应全 `[skip]`）；
5. **改 DAO + 重编译 `jt_db` + 补 `test_all_dao.py` 断言** → 之后才可删掉 `life.py:130` 的 Python 侧补查。

### 验收勾选表

- [ ] `db/sql/14~18_*.sql` 五个脚本都在 `dev`
- [ ] 连跑两遍输出已贴（第二遍全 `[skip]`，无报错）
- [ ] `information_schema` 查到 3 列 + `idx_deadline`（现为 0）
- [ ] `/health/detail` 的 `notice_deadline_supported` = **true**（现为 false）
- [ ] `backend/app/schemas/` 有这 3 个字段（现为 0）
- [ ] `LifeDAO.page_notices` SELECT 含这 3 列 + `test_all_dao.py` 有断言
- [ ] `pytest tests -q` 全绿（**当前 3 failed / 69 passed**）
- [ ] `14_notice_extend.sql` 已从 #60 移除（由 B19 打包 PR 承接）

> 🔴 **全队意义**：`B19` 是 W1 **最长阻塞链** —— 卡 `C22~C25`（改表 + 重编译，成员3）与 `B21~B24`（成员2），再往下卡 `F13`/`F17`（成员1）。
> ⇒ **B19 应独立成 PR 优先合**，不要再挂在 B16/B17/B18 上等一起过。

---

## 4. 🟡 仍开放（不阻断，但请排期）

| # | 项 | 说明 |
|---|---|---|
| P2-2 | 上传解析通道**先整体缓冲后判 32MB** | 同仓 `upload.py:51` 已有 `_read_limited()` 未复用 |
| P2-3 | PDF 解压上限在 `zlib.decompress()` **之后**才判 | 主路径也应传 `max_length=_MAX_STREAM_BYTES` |
| P2-4 | `total` 仍是**本页条数**（`paged(items, len(items), page, size)`） | 与审计 `DATA-06` 同族；建议 SQL 侧同条件 `COUNT(*)` |
| P3-1 | `delete_doc` 向量删除异常 `except Exception: pass` 全吞 | 建议 `logger.warning` + 清 `rag` 的 60s TTL 缓存 |
| P3-2 | `purge_by_source_prefix` 逐条删、**非单事务** | 响应带 `partial` 标记 |

---

## 5. 合并前后注意事项

- `dev` ← 本分支 **无冲突**（`merge-tree` 干净）；落后 16 会因为合并自动前进，**不影响合并**；
- 但按规则集：**新提交会作废批准** ⇒ 修完 P1 + 让 `pytest` 全绿 → Approve → **立刻合并**；
- ⚠️ 本分支新增的 `14_notice_extend.sql` **只有这一份**，与 §3.4 的 `B19` 是同一件事；合入后请同步任务总表状态。

## 6. 复现命令

```powershell
git fetch github "+refs/pull/60/head:refs/remotes/github/pr60"
git worktree add --detach $env:TEMP\pr60 github/pr60
Copy-Item <主树>\backend\app\db\native\* $env:TEMP\pr60\backend\app\db\native\ -Force

cd $env:TEMP\pr60\backend
$env:XJT_DB_PASSWORD='<pwd>'; $env:XJT_DB_PORT='3307'
E:/miniconda3/python.exe -m pytest tests -q          # 期望全绿；当前 3 failed / 69 passed
```

# 今日任务单 · 2026-09-15 · 成员2 & 成员3

> **硬目标**：**成员2 今天把 `B20`~`B23` 全部干完并合入 `dev`**｜成员3 清掉 `C22`~`C25` 并解决仓库/环境阻塞。
> **前提**：`B19` 的 DDL **已经在库里执行完了**（见 §1），所以 `B20`~`B23` 的**表结构障碍已清零**，今天纯拼代码。
> 所有"现状"均为 **2026-09-15 实测**（本地库 + 本地 git），不依赖网络。

---

## 0 · 🔴 开局必须先解决的 3 个环境阻塞（不解决，今天一行代码都交不上去）

| # | 阻塞 | 症状（实测） | 谁来解 | 解法 |
|---|---|---|---|---|
| E1 | **GitHub 代理没开** | `git fetch` → `Failed to connect to github.com:443 over proxy 127.0.0.1`（EXIT=128） | **全员** | 启动代理（`127.0.0.1:7897`）后再操作；否则**推不了、开不了 PR** |
| E2 | **`fix/task-sheet-v4` 7 个提交只在本地** | `git branch --list` 有、远端无 | 成员3 | 代理恢复后 `git push github fix/task-sheet-v4`，再建 `?base=dev` 的 PR |
| E3 | **GitKraken PR 工具被禁用** | 调用 PR 接口报 "disabled by the user" | 全员 | 在 VS Code 里重新启用该 MCP，否则**只能命令行推代码、网页开 PR** |

> ⚠️ **E1 优先级最高**：它让所有人的提交都堵在本地。**在做任何编码前先确认 `git ls-remote github` 能通（EXIT=0）。**
> 离线期间唯一能做的：本地编码 + 本地测试 + 本地 commit（**不能 push**）。

---

## 1 · 基线事实（实测，2026-09-15）

### 1.1 `B19` DDL 已执行完毕 ✅（昨天的成果，库上已生效）

```
[B19] home_banner            = 1（表已建，含 3 条轮播种子）
[B19] user_search_history    = 1（表已建，含 uk_user_keyword 去重约束）
[B19] pickup_point           = 1（表已建，含 3 条驿站种子）
[B19] user.student_no 可空?  = YES      ← 已由 NOT NULL 改为可空
[B19] user.token_version + student_no_updated_at = 都存在
[B19] takeaway_order 新列    = biz_type,pickup_code,pickup_point_id,arrived_at,notified_at（5 个都在）
```

**⇒ `C22`/`C23`/`C24`/`C25`/`B21`/`B22`/`B23` 的表结构障碍全部清零，今天不用再等 DDL。**

### 1.2 `B20` 的 DDL **仍未执行** ❌

```
[B20] campus_notice 扩展列 = (一个都没有)
[B20] idx_deadline         = 0
```

### 1.3 分支位置（离线可测，基线 = 本地 `github/dev`）

| 分支 | @commit | 落后/领先 dev | 说明 |
|---|---|---|---|
| `fix/task-sheet-v4` | `4c1c9ef` | 0 / **7** | 昨天全部文档；**未推远端** ⚠️ |
| `feat/b19-ddl-pack` | `a9ef738` | 0 / 1 | **PR #74**（B19 的 15~18） |
| `feat/c15-chunk-strategy` | `3bc6c90` | 4 / 4 | **PR #63** |
| `feat/c20-citation-check` | `889c45a` | 4 / 4 | **PR #65** |
| `test/beta-assets` | `b33feea` | 17 / 8 | **PR #69**（base 是 `main` ❌ 要改 `dev`） |
| `fix/guard-sandbox-utils` | `3ac506b` | 4 / 1 | **PR #70**（base 是 `main` ❌ 要改 `dev`） |

---

## 2 · 成员2 · 今日任务（`B20` → `B21` → `B22` → `B23`，全天满负荷）

> **建议顺序**：`B20`（先把 DDL 与 DAO 顺序做对）→ `B21`/`B22`（**表已就绪，最快出成果**）→ `B23`（等成员3 上午交付 C22 的 DAO 后接上）。
> 时间分配建议：`B20` 上午、`B21`+`B22` 中午~下午两点、`B23` 下午。

### B20 · 通知表结构扩展（`campus_notice` +3 列）⭐

| 项 | 内容 |
|---|---|
| **现状** | `14_notice_extend.sql` 已写在 `feature/backend`（PR #60）；**库里三列与 `idx_deadline` 都不存在** |
| **今天做什么** | ① 让 `14_notice_extend.sql` 进 `dev`（推 #60 合并，或单独开 PR 交付该脚本）<br>② **执行 DDL**：`mysql xiaojietong < 14_notice_extend.sql`，**连跑两遍**（第二遍应全 `[skip]`）并贴输出<br>③ 改 `LifeDAO::page_notices` 的 SELECT 加 `deadline/materials/importance` → `cmake --build db/cpp_driver/build --config Release --target jt_db` → 补 `test_all_dao.py` 断言<br>④ **删掉** `routers/life.py:130` 的 Python 侧补查（`notice_service.attach_extended_fields(items)`）<br>⑤ 给 `notice_scheduler.notice_extended_columns()` 探测失败时补 **`logger.warning`** |
| **验收** | `information_schema` 查到 3 列 + `idx_deadline`；`/health/detail` 的 `notice_deadline_supported` = **true**；`pytest tests -q` 全绿；`test_all_dao.py` 有新列断言 |
| **⚠️ 顺序铁律** | **必须先执行 DDL、再改 DAO**。反过来会让 `/life/notices` **直接 SQL 报错**（列不存在却去 SELECT）——当前"列不存在就不 SELECT"的降级设计是**有意的，别提前改**。 |
| **参考** | PR #60 的「合并复核」评论；PR #73（已合入的契约测试 `tests/test_notice_extend_contract.py`） |

### B21 · 首页数据接口（`GET /home/banners` + `GET /home/feed`）

| 项 | 内容 |
|---|---|
| **现状** | `routers/home.py` **不存在**；**`home_banner` 表已在库里**（3 条种子）✅ |
| **今天做什么** | 新建 `backend/app/routers/home.py`：<br>· `GET /home/banners` → 过滤 `enabled = 1 AND (start_at IS NULL OR start_at <= NOW()) AND (end_at IS NULL OR end_at >= NOW())`，`ORDER BY sort DESC`<br>· `GET /home/feed` → 聚合（论坛热帖 + 通知 + 活动），支持排序 + 分页<br>· 两个接口都要**鉴权**；在 `docs/api.md` 登记；补测试 |
| **验收** | 两接口 `code=0`；feed 支持排序+分页；有鉴权；轮播按有效期与 `sort` 正确排序 |
| **⚠️ 契约** | `15_home_banner.sql` 里 `sort` 的约定是**越大越靠前**（`ORDER BY sort DESC`）。若你要升序，**必须同时改 `15_` 的注释与种子**，别让脚本和代码各说一套。 |
| **可用种子** | 3 条：校园通知新版上线(30) / AI 助手帮你答疑(20) / 二手交易更放心(10) |

### B22 · 搜索历史接口（`GET`/`POST`/`DELETE /search/history`）

| 项 | 内容 |
|---|---|
| **现状** | `routers/search.py` **不存在**；**`user_search_history` 表已在库里**（含 `uk_user_keyword` 唯一键）✅ |
| **今天做什么** | 新建 `backend/app/routers/search.py`：新增（去重）/ 倒序拉取 / 单删 / 清空；`docs/api.md` 登记；补测试 |
| **验收** | 写入**自动去重**；倒序拉取；清空生效 |
| **🔴 必看的契约** | 表上有 `uk_user_keyword (user_id, keyword)` ⇒ **「再次搜索同一个词」不能裸 `INSERT`**，否则 `ERROR 1062`。必须写：<br>`INSERT INTO user_search_history (user_id, keyword) VALUES (?, ?) `<br>`ON DUPLICATE KEY UPDATE created_at = CURRENT_TIMESTAMP;`<br>这样「自动去重」+「倒序拉取」两条验收口径才同时成立。 |

### B23 · 用户资料增强（`PUT /user/me` 支持 `student_no`）

| 项 | 内容 |
|---|---|
| **现状** | 列已就绪：`student_no` **可空** + **`uk_student_no` 唯一索引** + `student_no_updated_at`（限频）+ `token_version` ✅ |
| **今天做什么** | 改 `backend/app/routers/user.py`：<br>① 支持写 `student_no`，**重复学号要拒**（靠 `uk_student_no`，捕获 1062 转业务错误）<br>② **限频 1 次 / 7 天**：用 `student_no_updated_at` 判定，超频返回 **`3001` + 剩余天数**<br>③ **格式按学校配置校验**：调 `app/adapters`（C21 已合入 `dev`），不要写死规则<br>④ `docs/api.md` 登记两类错误；补测试 |
| **验收** | 重复学号被拒；超频返回 `3001` + 剩余天数；格式规则**由适配器配置提供**（换学校配置即改行为） |
| **⚠️ 依赖** | 需要成员3 **上午**交付的 `C22` DAO（见 §3）。**B23 的接口骨架与格式校验可以先并行写**，只把"限频判定 + 落库"那一步留到 C22 交接后接上。 |
| **⚠️ 契约** | 老 token 里没有 `token_version` ⇒ **缺省按 `0` 处理**，否则会把已登录用户全部踢下线。 |

---

## 3 · 成员3 · 今日任务（`C22`→`C25` + 清阻塞）

> **上午优先做 `C22`**（它是成员2 的 `B23` 的前置），做完**立刻通知成员2**；下午做 `C23`/`C24`/`C25`。

### C22 · 学号唯一 + 限频 + `token_version`（DAO 层 + 重编译）→ **上午优先**

| 项 | 内容 |
|---|---|
| **现状** | 表结构**已完成**（我的 `17_user_student_no.sql` 已执行）：`student_no` 可空、`uk_student_no` 唯一、`student_no_updated_at`、`token_version` 都在；空串学号已清洗（**实测 64 行**，任务单写 63） |
| **今天做什么** | ① `user_dao` 加学号相关 DAO：`update_student_no(user_id, student_no)`（同时写 `student_no_updated_at`）、`bump_token_version(user_id)`、读 `token_version`<br>② `core/deps.get_current_user` 接入 `token_version` 校验（**老 token 缺字段按 0**）<br>③ 重编译：`cmake --build db/cpp_driver/build --config Release --target jt_db`<br>④ 补 `test_all_dao.py` 断言 |
| **验收** | 重复学号被 DB 唯一键拒；限频时间戳可读；登出后旧 token 失效（新 token 可用） |
| **交接** | **上午完成后立刻告诉成员2**："C22 DAO 可用"，让他接 `B23` |

### C23 · `user_search_history` 表 + DAO → 下午

| 项 | 内容 |
|---|---|
| **现状** | **表已在库里**（`uk_user_keyword` + `idx_user_created`）✅ |
| **今天做什么** | DAO：增（**`ON DUPLICATE KEY UPDATE`** 去重）/ 查（按 `(user_id, created_at)` 倒序）/ 单删 / 清空 |
| **验收** | 写入去重；按 `(user_id, created_at)` 查询走索引（`EXPLAIN` 无全表扫） |
| **⚠️ 与 B22 的边界** | DAO 归我、接口归成员2。**先定好方法签名再各自动手**，避免重复实现。 |

### C24 · `home_banner` 表 + DAO → 下午

| 项 | 内容 |
|---|---|
| **现状** | **表已在库里**（11 列 + `idx_enabled_sort` + 3 条种子）✅ |
| **今天做什么** | DAO：list（`enabled` + `start_at`/`end_at` 有效期过滤 + `sort DESC`）/ 增删改 |
| **验收** | 支持 `enabled` + `start_at`/`end_at` 过滤；排序正确 |
| **⚠️ 与 B21 的边界** | DAO 归我、接口归成员2。`sort` 方向 = **越大越前**（与 `15_` 一致），**接口层不要另立一套语义**。 |

### C25 · 代收闭环数据改造 → 下午

| 项 | 内容 |
|---|---|
| **现状** | **`pickup_point` 表已在库里**（3 驿站种子）；`takeaway_order` 已加 `biz_type`/`pickup_code`/`pickup_point_id`/`arrived_at`/`notified_at`；**历史回填已完成（3 行全为 `biz_type=1` 代买，代收 0 行）** ✅ |
| **今天做什么** | DAO：`pickup_point` 增查；`takeaway_order` 的 `biz_type`/取件码生成与回查/到件标记；**新订单默认代收（`biz_type=2`，DB 默认值已设）**；重编译 |
| **验收** | 历史订单不丢（3 行仍为代买）；新订单默认代收；取件码可生成与回查 |
| **⚠️ 注意** | 「代收**不计费**」——金额字段保留但不要引入支付流程（方案文档 L304）。 |

### 成员3 · 仓库/环境清理（穿插做）

- [ ] **E2**：代理恢复后立刻 `git push github fix/task-sheet-v4`（7 个提交，昨天全部文档）
- [ ] `feat/b19-ddl-pack`（**PR #74**）确认已推远端并 mergeable；跟进评审
- [ ] **`#69` / `#70` 的 base 从 `main` 改成 `dev`**（已错 4 次的老问题）
- [ ] `feat/c15-chunk-strategy`（**PR #63**）、`feat/c20-citation-check`（**PR #65**）从 `dev` 对齐后再找人 Approve
- [ ] 代理恢复后**同步本地 `dev`**，并让 `feature/backend` 从 `dev` 回合并

---

## 4 · 交接点与时间线

```
上午                中午                 下午                     收尾
──────────────────────────────────────────────────────────────────────────────
成员3: C22 DAO      C23 DAO             C24 DAO + C25 DAO        推分支/清 PR base
       + 重编译      │                   │                        └─ 通知全体对齐 dev
       ↓ 通知         ↓                   ↓
成员2: B20 顺序铁律  B21 首页接口        B22 搜索历史              B23 用户资料增强
      (DDL→DAO)     (+ C24 DAO 对接)     (+ C23 DAO 对接)          (+ C22 DAO 对接)
──────────────────────────────────────────────────────────────────────────────
⚠️ 硬依赖：成员2 的 B23 等成员3 的 C22（上午）；C23/C24 的 DAO 与接口各自对接一次即并行
```

---

## 5 · 今日共同验收清单（当日收工时逐条勾）

### 环境
- [ ] `git ls-remote github` **EXIT=0**（代理已恢复）
- [ ] `fix/task-sheet-v4` 已推远端；`#69`/`#70` 的 base 已改 `dev`

### 成员2（`B20`~`B23` 全绿）
- [ ] `campus_notice` 3 列 + `idx_deadline` 存在；`/health/detail` 的 `notice_deadline_supported` = **true**
- [ ] `LifeDAO::page_notices` SELECT 含 3 列；`jt_db` 已重编译；`test_all_dao.py` 有断言
- [ ] `pytest tests -q` **全绿**（含 `test_notice_extend_contract.py` 的 11 条）
- [ ] `GET /home/banners` / `GET /home/feed` 返回 `code=0`、有鉴权、`docs/api.md` 已登记
- [ ] `GET/POST/DELETE /search/history` 去重生效、倒序、清空生效、`docs/api.md` 已登记
- [ ] `PUT /user/me` 重复学号被拒；超频返回 `3001` + 剩余天数；格式走适配器配置

### 成员3（`C22`~`C25` 全绿）
- [ ] `C22`：DAO + `token_version` 校验 + 重编译 + `test_all_dao.py` 断言
- [ ] `C23`：`user_search_history` DAO（去重用 `ON DUPLICATE KEY UPDATE`）
- [ ] `C24`：`home_banner` DAO（有效期 + `sort DESC`）
- [ ] `C25`：`pickup_point` + 代收 DAO；历史 3 行仍为代买；新订单默认代收
- [ ] `pytest tests -q` 全绿；`jt_db` 重编译通过

---

## 6 · 今日已知风险（提前盯）

| 风险 | 影响 | 预案 |
|---|---|---|
| 代理持续不通 | **所有人交不了代码** | 先本地 commit 攒着；恢复后批量推；必要时用 gitee 镜像 |
| `B20` 顺序做反（先改 DAO 后跑 DDL） | `/life/notices` **直接 SQL 报错**，接口全挂 | 严格按 §2-B20 的 ①→⑤ 顺序；DDL 跑完先查 `information_schema` 再动 C++ |
| `B22` 忘写 `ON DUPLICATE KEY UPDATE` | 重复搜索报 `1062`，验收直接不过 | 见 §2-B22 的契约；写一条"同人同词搜两次"的测试 |
| `B23` 老 token 不按 0 处理 | **全站用户被踢下线** | 见 §2-B23 契约；加一条"旧 token 无该字段仍可登录"的测试 |
| `C22` 交付晚于中午 | 连带 `B23` 一起拖到明天 | 成员3 上午**只做 C22**，做完立刻通知；成员2 先写 B23 的格式校验骨架 |
| 多人同时改 `routers/user.py`（`C22` 与 `B23` 都碰） | 冲突 | 约定：**成员3 只改 `user_dao`/`deps`，成员2 只改 `routers/user.py`**，不越界 |

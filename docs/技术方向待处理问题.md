# 校捷通 · 技术方向待处理问题清单

> **定位**：本清单收录**不适合塞进当前迭代批次**的技术问题 —— 即那些需要独立排期、
> 需要跨人协作、需要改表结构/改 C++ 接口、或需要产品决策才能推进的项。
>
> **维护约定（重要）**：
> 1. **凡是被判定为"单独批次处理"的问题，一律追加到本清单**，不得只留在聊天记录或临时文档里；
> 2. 每项必须写清：**为什么单列**（依赖/风险/前置条件）、**建议做法**、**验收方式**；
> 3. 解决后在「状态」列标记 `✅ 已解决` 并注明 commit，**不删除条目**（保留决策痕迹）；
> 4. 来源可追溯：审计发现标 `审计`，开发中发现标 `开发`，产品/部署需求标 `规划`。
>
> **数据来源**：`docs/项目审计报告260910序2-技术专项.md`（46 项风险：P0×11 / P1×18 / P2×17）
> ｜ 建立日期 2026-09-11 ｜ 维护人：成员3
>
> **当前总进度**：P0 **11/11 已闭环** ｜ P1 **8/18 已闭环** ｜ P2 **0/17**

---

## A. 安全与权限（P1 剩余）

| 编号 | 来源 | 问题 | 为什么单列 | 建议做法 | 验收方式 | 状态 |
|---|---|---|---|---|---|---|
| **SEC-07** | 审计 | 登出完全无效（no-op）：`/auth/logout` 直接返回成功，无黑名单也无 token 版本号；被盗 token 在 2h 有效期内无法作废 | **需改 `user` 表加列**（`token_version`），涉及 C++ `user_dao` SELECT 补字段 → 必须**全员同步执行 SQL + 重编译 `jt_db.pyd`**，不能单方面推 | ① `user` 表加 `token_version INT NOT NULL DEFAULT 0`；② JWT payload 带 `ver`；③ `deps.get_current_user` 校验 `ver == user.token_version`；④ `logout` 执行 `token_version = token_version + 1` | 登出后旧 token 调 `/user/me` 返回 401/未授权；`/health` 或测试脚本可复现 | ⬜ 待处理 |
| **SEC-08** | 审计 | Refresh token 可重复使用、无轮换、无吊销；`refresh()` 时不校验账号 `status`/`is_deleted` | 与 SEC-07 同源（依赖 `token_version`），且**轮换策略需产品确认**（是否允许并发设备登录、宽限期多长） | 刷新时①校验账号状态；②签发新 refresh 并作废旧的（配合 `token_version` 或引入 `refresh_jti` 表） | 旧 refresh 二次使用被拒；被禁用账号刷新被拒 | ⬜ 待处理 |
| **SEC-09** | 审计 | `role >= 1` 即管理员，`role=2` 与 `role=1` 无能力差异；高风险能力（全量重建索引）对**全部管理员**开放 | **需产品侧先拍板角色模型**（是否要超管/运营/审计分权），代码改早了会返工 | 明确角色枚举与权限矩阵 → `deps.py` 增加 `require_role(...)` 依赖 → 高风险接口单独收紧 | 权限矩阵文档 + 越权用例被拒 | ⬜ 待处理（待产品确认） |

---

## B. 并发与事务（P1 剩余 + P2 全部）

| 编号 | 来源 | 问题 | 为什么单列 | 建议做法 | 验收方式 | 状态 |
|---|---|---|---|---|---|---|
| **CON-03** | 审计 | 事务块位于 `async def` 内直接调用同步 DB，阻塞事件循环（P0 只修了 RAG 降级路径） | 涉及面广（多个路由），逐处改造需回归，宜整体一批处理 | 统一改为 `await asyncio.to_thread(...)` 包裹，或把 DB 访问收敛到线程池执行 | 并发压测下事件循环延迟不再尖刺 | ⬜ 待处理 |
| **CON-04** | 审计 | `like_count` / `comment_count` 与明细表可能不一致（先写明细再改计数，中途失败即漂移） | 需要**对账策略**（定时修复 vs 事务强一致），属架构选择 | 二选一：①同一事务内更新计数；②定时对账任务修正 | 人为制造中断后计数可自愈/不一致为 0 | ⬜ 待处理 |
| **TXN-03** | 审计 | `favorite` / `like` 并发 toggle → 唯一键冲突 → 500（用户体验差） | 需区分"重复收藏"与"真冲突"，且要考虑幂等语义 | 捕获唯一键冲突（错误码 1062）转为幂等成功；或用 `INSERT ... ON DUPLICATE KEY` | 并发 toggle 压测无 500 | ⬜ 待处理 |
| **TXN-04** | 审计 | 事务边界与语义问题（4 处）：事务内包含非 DB 操作、长事务持有连接等 | 需逐处确认业务语义，宜与对应业务负责人一起过 | 逐处列出 → 缩短事务持有时间 → 把外部 IO 移出事务 | 事务内无网络调用 | ⬜ 待处理 |
| **TXN-05** | 审计 | **未显式设置隔离级别**（全仓 `SET TRANSACTION ISOLATION LEVEL` 0 命中），依赖 MySQL 默认 RR | **属全局架构决策**，改隔离级别会影响所有并发行为，需整体回归 | 显式设为 `READ COMMITTED`（配合关键场景行锁），并写入 `architecture.md` | 配置文件/连接初始化中可见显式设置 | ⬜ 待处理 |
| **TXN-06** | 审计 | `commit()/rollback()` 失败分支先置 `in_transaction_=false` 再抛异常 → 连接归还时不再回滚，MySQL 侧事务可能未结束 | **改 C++ `mysql_connection.cpp`**，需重编译 + 全员同步；影响面涉及连接池状态机 | 调整顺序：仅在整个清理流程成功后才置标志位；失败时标记连接为"不可复用"并销毁 | 模拟 commit 失败后连接不被复用/事务确实回滚 | ⬜ 待处理 |
| **TXN-07** | 审计 | `user.uk_openid` 未被应用层利用：`auth.py` 先查后建，并发注册存在竞态（可能产生重复用户或 500） | 需与 SEC-07/08 一并在认证模块改造中处理 | 捕获唯一键冲突后改为「查询已存在用户」；或用 `INSERT ... ON DUPLICATE KEY UPDATE` | 并发同 openid 登录只产生一个用户 | ⬜ 待处理 |
| **CON-05** | 审计 | 全仓 `FOR UPDATE` / `LOCK IN SHARE MODE` / `GET_LOCK` / `ON DUPLICATE KEY` **0 命中**（悲观锁使用经验缺失，P0 已补 2 处） | 属工程习惯问题，需在团队内统一规范后逐步落实 | 编写《并发场景对照表》：什么场景用行锁、什么场景用幂等键 | 评审 checklist 生效 | ⬜ 待处理 |
| **CON-06** | 审计 | `prune_idle()` 是死代码，被 `wait_timeout` 掐断的连接长期滞留池中 | **改 C++ 连接池**，需重编译 | 接入维护线程周期调用，或归还时探测连接有效性 | 连接池空闲数不再包含失效连接 | ⬜ 待处理 |
| **CON-07** | 审计 | 归还回调捕获 `this` + `thread_local txn_`，静态析构顺序未定义 → **理论 UAF** | **C++ 生命周期改造**，风险高需谨慎验证 | 用 `weak_ptr`/`enable_shared_from_this`；`thread_local` 改显式传参 | 静态分析器无告警 + 长稳压测无崩溃 | ⬜ 待处理 |
| **CON-08** | 审计 | `init()` 可重复调用导致 `active_` 计数错乱 | 改 C++，与 CON-06/07 同批 | 加一次性初始化保护（`std::once_flag`） | 重复 init 后计数仍正确 | ⬜ 待处理 |

---

## C. 缓存、检索与性能（P1 剩余 + P2 全部）

| 编号 | 来源 | 问题 | 为什么单列 | 建议做法 | 验收方式 | 状态 |
|---|---|---|---|---|---|---|
| **CAC-05** | 审计 | 无索引覆盖的查询：`LIKE '%关键词%'` 前缀通配导致全表扫；表达式排序无法走索引 | **属索引结构改造**：需引入 `FULLTEXT ... WITH PARSER ngram` 或改为向量检索前置，改动大 | ① 对 `knowledge_doc.content`、`topic.title/content` 建 ngram 全文索引；② 降级路径改用 `MATCH ... AGAINST`；③ 保留 `LIKE` 作为兜底 | `EXPLAIN` 显示不再 `type=ALL` | ⬜ 待处理 |
| **CAC-09** | 审计 | `requirements.txt` 声明 `redis/celery` 但**零使用**（依赖臃肿、部署误导） | 需先定"是否真的要用 Redis/Celery"，属技术选型 | 二选一：删除未用依赖，或按规划真正接入 | `pip install -r` 后无冗余依赖 | ⬜ 待处理 |
| **CAC-10** | 审计 | 静态资源无 `Cache-Control`；上传文件名带时间戳 → **同图重复上传产生多份文件** | 小改动但需与前端约定缓存策略 | 文件名去掉时间戳（纯 md5）；静态响应加长 `Cache-Control: immutable` | 同图两次上传返回同一 URL | ⬜ 待处理 |
| **CAC-11** | 审计 | 无 API 响应缓存/ETag（列表类接口每次都回源） | 需先明确哪些接口可缓存、失效策略如何 | 对只读列表接口加 ETag 或短 TTL 缓存 | 命中缓存时返回 304/低延迟 | ⬜ 待处理 |
| **CAC-12** | 审计 | `report` / `navigation_log` 可被无限灌入（表膨胀） | 需配限流策略（SEC-10 已有限流中间件可复用） | 对写接口单独设更严的限流阈值 + 定期归档 | 单用户高频写入被拦截 | ⬜ 待处理 |
| **CAC-13** | 审计 | 长任务（索引重建、知识入库）走在线 HTTP 同步请求，与用户流量抢连接池 | 属**任务队列架构**改造（需引入 Celery 或后台线程池） | 长任务改异步提交 + 进度查询接口；`build_index` 已在 CLI 侧可独立跑 | 重建期间 `/chat` 延迟不受影响 | ⬜ 待处理 |
| **CAC-14** | 审计 | 向量库初始化在**请求线程内**构造（Chroma 打开文件句柄在锁内）→ 首请求变慢且阻塞其他线程 | 小改动，但需确认启动顺序 | 在 `main.py` lifespan 中**预热** `get_vector_store()` | 首个检索请求延迟与后续持平 | ⬜ 待处理 |

---

## D. 数据校验与体验（P2）

| 编号 | 来源 | 问题 | 为什么单列 | 建议做法 | 验收方式 | 状态 |
|---|---|---|---|---|---|---|
| **SEC-15** | 审计 | `GET /topics/{id}` 浏览量与举报无节流/无去重（已部分缓解：浏览量改聚合，但**无按用户去重**）；`cancel_task` / `mark_notice_read` 不检查 `affected` → 返回「假成功」 | 前者需"按用户去重"的存储（内存或 Redis）；后者是接口语义修正，需逐个过 | ① 浏览量按 `(user, topic)` 去重；② 所有 `UPDATE`/`DELETE` 检查 `affected`，为 0 时返回 404/业务错误 | 取消不存在的任务返回明确错误 | ⬜ 待处理 |
| **SEC-16** | 审计 | 输入长度/类型校验普遍缺失；⚠️ **`library.py:47-51` 时间用字典序比较**（`"9:00" >= "10:00"` 为真 → **时段校验逻辑错误**）；`secondhand.py:119` 裸 `dict` body | 时间比较是**真实逻辑 bug**，建议**优先单独修**（成本低、影响明确） | ① 时间统一转 `time`/`timedelta` 比较（或补零 `"09:00"`）；② 各路由补 Pydantic 字段约束（`max_length`/`ge`/`le`） | 时段 9:00–10:00 的冲突判定正确 | ⬜ 待处理（含 1 个功能 bug） |

---

## E. 架构、限流与部署方向（规划）

| 编号 | 来源 | 问题 | 为什么单列 | 建议做法 | 验收方式 | 状态 |
|---|---|---|---|---|---|---|
| **ARCH-01** | 开发 | 限流为**单进程内存**实现：多 worker / 多机部署时各进程独立计数，不是全局精确限流 | 需 Redis 等共享存储，属部署架构决策 | 引入 Redis → 切换为集中式滑动窗口/令牌桶；保留内存实现作为无 Redis 时的降级 | 多 worker 下限流阈值全局生效 | ⬜ 待处理 |
| **ARCH-02** | 开发 | **数据库口令需轮换**：`jhq000000` 曾明文入库并推送公开仓库，虽已从工作区清除，但 **Git 历史中仍可检索到** | 需**全员协调**（改本机 MySQL + 各自 `.env`），单方面改会打断他人环境 | ① 各成员将本地 root 密码改为新值；② 密码只存 `backend/.env`（已在 `.gitignore`）或用户级环境变量；③ 文档统一用 `${XJT_DB_PASSWORD}` | 全仓 grep 无明文；旧密码无法连接 | ⚠️ 待全员执行 |
| **ARCH-03** | 规划 | 未容器化：无 Docker / Nginx / HTTPS / 域名（小程序**强制要求 https**） | 属部署工程，需要服务器资源与域名，非代码问题 | 编写 `deploy/` 下的 Dockerfile + compose + Nginx 反代 + certbot 流程 | 可通过 https 域名访问 Swagger | ⬜ 待处理 |
| **ARCH-04** | 规划 | Agent 长任务无异步队列，同步占用连接（与 CAC-13 同源） | 需引入 Celery/Redis 或 APScheduler，属技术选型 | 定选型 → 长任务（提醒推送、批量索引、AI 审核）改为队列执行 | 长任务不占用在线请求连接 | ⬜ 待处理 |
| **ARCH-05** | 开发 | 本地开发环境**配置分散**：`XJT_DB_PASSWORD` 需手工设环境变量（`.vscode/launch.json` 已改为 `${env:...}`，未设则 F5 调试失败） | 属开发者体验，需团队统一约定（`.env` 方案 vs 系统环境变量） | 推荐 `backend/.env`（已 gitignore）+ `python-dotenv` 自动加载；并写入 `DEVELOPMENT.md` | 新成员 clone 后按文档 5 分钟可跑 | ⬜ 待处理 |

---

## F. 功能与交付补全（规划）

| 编号 | 来源 | 问题 | 为什么单列 | 建议做法 | 验收方式 | 状态 |
|---|---|---|---|---|---|---|
| **FEAT-01** | 规划 | `ai/eval/` 独立评估集为空（现有 `eval_compare.py` 仅做基座 vs 微调对比） | 需人工构造标注集，非纯代码任务 | 构建 ≥50 条校园问答评测集 + 自动评分脚本 | 可输出准确率/幻觉率报告 | ⬜ 待处理 |
| **FEAT-02** | 规划 | `ai/edge/` 端侧 demo 为空（模型分发与部署脚本已就绪，但无端侧演示） | 需明确端侧目标（手机/边缘盒子） | 定目标平台 → 提供量化 + 推理 demo | 端侧可跑通问答 | ⬜ 待处理 |
| **FEAT-03** | 规划 | 多模态能力缺失：无语音输入/播报、无图像识别、无 LBS 定位 | 属新功能方向，需产品排期 | 按优先级拆解（语音输入 → 图像识别 → LBS） | 小程序端可演示 | ⬜ 待处理 |
| **FEAT-04** | 规划 | 小程序剩余页面（F3~F9）尚未开发 | 成员1 主责，属正常迭代而非阻塞项 | 按 `docs/成员任务单260910.md` 推进 | 页面可演示真数据 | 🚧 进行中 |
| **FEAT-06** | 开发 | **前端审查发现的 12 个问题**（详见 `docs/前端审查报告260911.md`，三轮审查）：<br>**第 1 轮（5 项，均未修）**：① `2003` 未跳登录页 ② **`BASE_URL` 硬编码 http → 上线阻塞** ③ TabBar 无图标 ④ `getApp()` 顶层 ⑤ `3003` 未处理<br>**第 2 轮（3 项）**：⑥ **F4 缺【停止】【+新会话】【历史】** ⑦ `onShow` 关键词被丢弃 ⑧ **座位抢占失败不刷新状态**<br>**第 3 轮 F5~F9 深化（4 项）**：⑨ **阅览室列表借用地图接口**（根因：后端缺 `GET /library/rooms`）⑩ **二手详情用 URL query 传数据**（根因：后端缺单项详情接口，且有 URL 超长风险）⑪ **点赞无防重复**（toggle 语义连点翻转）⑫ `getApp()` 顶层扩散至 3 处 | 需**成员1 前端改 + 成员2 补接口**；② 依赖域名与 HTTPS 部署（见 `ARCH-03`），方案见 `docs/前端上线-域名与HTTPS方案.md`；⑨⑩ 的**根因在后端缺接口**，成员1 已在注释中如实标注 | 前端：①`request.js` 纳入 `2003` ②新增 `config.js` 三环境 ③补 TabBar 图标 ④⑫ `getApp()` 移入生命周期 ⑤按需 ⑥加停止/新会话/历史页 ⑦调整清空顺序 ⑧`catch` 判 `3001` + 刷新座位 ⑪加 `liking` 标志<br>后端：⑨补 `GET /library/rooms`（按 `room_type=0` 过滤）⑩补 `GET /secondhand/items/{item_id}` | 禁用用户能跳登录页；真机可访问 https；对话可中断/新建/回看；座位抢占失败后界面刷新；详情页不再依赖 URL 传参 | ⬜ 待处理 |
| **FEAT-05** | 规划 | 交付文档缺 3 份：需求说明书、ER 图、AI 模型训练报告 | 成员4 主责 | 见任务单 D3/D4 | 文档入库 | 🚧 进行中 |

---

## 附录 A：已闭环项对照（保留痕迹，勿删）

| 批次 | 编号 | commit |
|---|---|---|
| P0 批次 | SEC-01~06、TXN-01~02、CON-01~02、CAC-01 | `caf17fa` → `d73031f`（合入 dev） |
| P1 第一批 | SEC-10、SEC-11、SEC-12、SEC-13 | `c651edc`、`809bb83` |
| P1 第二批 | SEC-14、CAC-02、CAC-03、CAC-06、CAC-07 | `c905460` |
| 测试健壮性 | 限流脚本配额自诊断 | `f7ca01b` |
| 协作合并 | 成员2 的 B6 内容审核闭环 + B8 可观测性 | `25f87cd` |

> 详细修复要点与**实测证据**见 `docs/项目审计报告260910序2-技术专项.md` §0.4 / §0.4.1。

## 附录 B：下一批的推荐取法

建议**按依赖关系打包**，而不是按编号顺序：

| 建议批次 | 包含 | 理由 |
|---|---|---|
| **批次 A（认证闭环）** | SEC-07 + SEC-08 + TXN-07 | 三者都要动 `user` 表与 `auth.py`，**一次改表 + 一次 C++ 重编译**即可全部完成，成本远低于分三次 |
| **批次 B（C++ 连接池加固）** | TXN-06 + CON-06 + CON-07 + CON-08 | 都在 `connection_pool.cpp` / `mysql_connection.cpp`，同一次重编译完成 |
| **批次 C（检索性能）** | CAC-05 + CAC-13 + CAC-14 | 都围绕 RAG/向量库，且 CAC-05 的索引改造需要与成员2 的审核检索协同 |
| **批次 D（孤立小修，可随时插入）** | SEC-16（时间比较 bug）、CAC-10、CAC-14 | 改动小、风险低、收益明确，适合在等其他人时顺手做 |

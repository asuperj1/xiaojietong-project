# PR #39 回复评论（可直接粘贴到 GitHub）

> **用途**：粘贴到 [PR #39](https://github.com/asuperj1/xiaojietong-project/pull/39) 的评论框，回复 `dream-facing` 08:32 提出的两个待确认问题。
> **AppID 决策**：负责人已确认采用 **`wx6ccc5c4c02b31455`**（2026-09-12）。
> 复制下方分隔线之间的全部内容即可。

---

@dream-facing 两个问题都已确认，结论如下 👇

## A. AppID —— 已拍板：统一使用 `wx6ccc5c4c02b31455`

依据：它与《前后端连接性与接口审查报告260912》§203 的记录一致；而你本机的 `wxdac97100002f8adf` 在全仓文本检索与 `git log --all -S` 全历史检索中**零命中**，属个人本地配置。

请这样处理：

1. `miniprogram/project.config.json` → `appid` 改为 `wx6ccc5c4c02b31455`，然后**提交入库**
2. 顺带把 `urlCheck` 从 `false` 改回 `true`（上线前必须；现在改可提前暴露域名校验问题）
3. 说明：`appid` 属公开信息，入库无安全风险；但 **AppSecret 绝不能入库**，它只由后端 `XJT_WX_SECRET` 环境变量注入

⚠️ **一处更正**：`project.private.config.json` **不要入库**（含个人调试偏好，会与他人冲突）。
我上次说的「两个配置」意思是「提交 public 版 + 忽略 private 版」：

```bash
# 1) 若已加入暂存区，先移出（未跟踪则跳过本行）
git rm --cached miniprogram/project.private.config.json

# 2) .gitignore 追加（当前 .gitignore 里确实没有这条规则）
# ---- 微信开发者工具个人配置（勿入库）----
miniprogram/project.private.config.json
```

## B. TabBar 图标 —— 本 PR 不做，另开一个小 PR

理由（均已实测）：

- `ui/icons/navigation/` **缺 `user-solid.svg`**（「我的」选中态），5 组只有 4 组完整
- `miniprogram/static/icons/*.png` 是 **192×192 彩色插画**（给首页宫格用），不符合 tabBar 的 **81×81** 规范，也没有选中态
- `ui/` 目录按 `docs/分支规划与文件归属.md` 归 **`feature/ui`** 分支；SVG→PNG 属设计资源交付工序，放进本 PR 会造成跨模块耦合

**建议**：本 PR 保持文字 tabBar（**零功能影响**，不影响任何验收项）。
另开 `feat/ui-tabbar-icons` 小 PR：ui 出 10 个 81×81 PNG（顺带补 `user-solid.svg`）→ 前端只改 `app.json` 的 10 行（`iconPath` / `selectedIconPath`）。

## C. 意见③ 我这边修正了，本 PR 不必改

复核 `services/request.js` 后确认：业务错误与网络错误**都已统一 `wx.showToast`**，用户不会「无反馈」；`.catch(() => {})` 的真实影响仅是页面侧状态恢复的一致性。
**这条降级为后续代码约定，不需要你改。** 此前表述过重，抱歉。

## D. 合并前另一件事（与你无关，仅记录在案）

`services/request.js:8` 的 `BASE_URL` 仍写死 `http://127.0.0.1:8000/api/v1` —— 它**不阻断合入 `dev`**，但**阻断 `dev → main` 上线**，已列入「上线前必办」清单。

---

补完 **A** 之后直接 push 到 `feature/frontend`，PR 会自动更新，我再 review 一次 ✅

---

## 附：本次评论的事实依据（供团队留档）

| 结论 | 证据 |
|---|---|
| AppID 用 `wx6ccc5c4c02b31455` | `xjt-frontend/miniprogram/project.config.json` 的 `appid`；`docs/前后端连接性与接口审查报告260912.md:203` |
| `wxdac97100002f8adf` 是个人配置 | `git log --all -S` 零命中；全仓文本检索零命中 |
| 后端当前不校验 appid | `backend/.env` 不存在、`.vscode/launch.json` 无 `XJT_WX_APPID` → 走 mock 登录 |
| `.gitignore` 缺规则 | `Select-String .gitignore -Pattern 'project'` 零命中 |
| `user-solid.svg` 缺失 | `ui/icons/navigation/` 下仅有 `user.svg`（另有 `user-group.svg`/`user-group-solid.svg`） |
| 现有 PNG 为 192×192 | 读取 10 个 `static/icons/*.png` 的 PNG 头，宽高均为 (192, 192) |
| tabBar 需 81×81 | 微信小程序官方规范（`iconPath`/`selectedIconPath`，不支持 SVG / 网络图） |
| 空 catch 不影响用户感知 | `services/request.js` 业务错误分支与网络错误分支均调用 `wx.showToast` |

---

# 第二轮回复（针对新提交 `40e5248`，审查通过）

> 作者已于 08:49 推送 `40e5248 chore(miniprogram): add shared WeChat project config` 并回复「麻烦再 review 一次」。
> 以下是复核结论，**可直接粘贴到 PR 评论**。

```markdown
@dream-facing 复核完了，A 项 7/7 全部落实，PR 可以合并了 ✅

**已逐项验证**
- `appid` = `wx6ccc5c4c02b31455` ✅
- `urlCheck` = `true` ✅
- `project.config.json` 已入库、`project.private.config.json` **未入库** ✅
  （用 `git ls-tree` 确认该提交下 `miniprogram/` 只返回 `project.config.json`）
- `.gitignore` 规则**实测生效**：在临时工作树里执行
  `git check-ignore -v miniprogram/project.private.config.json` → 命中 `.gitignore:47`，exit=0 ✅
- 全文无 secret / appSecret 字样 ✅
- 合并性：`dev` 已是本 PR 的祖先（`git merge-base --is-ancestor` exit=0），**无冲突可干净合并** ✅

**两条 P3 建议（不阻断合并，你判断是否顺手补）**

入库这份是「新建精简版」，比开发者工具生成的完整版少了几个字段，其中 3 个有实际影响：

| 缺失字段 | 影响 | 建议值 |
|---|---|---|
| `libVersion` | 基础库版本改由工具默认决定 → 成员之间可能不一致 | `3.5.5` |
| `projectname` | 开发者工具里显示目录名（轻微） | `xiaojietong` |
| `setting.uploadWithSourceMap` | 上传体验版/正式版不带 sourcemap → 线上报错无法还原源码行号 | `true` |

> 另外你把 `minified` 从 `false` 改成 `true` 是**对的** 👍 上传自动压缩，包体积更小。

这 3 项**不阻断合并**：负责人想尽快落 `dev` 的话可以直接合，之后开个 tiny commit 补上；
要现在补也就 2 分钟。

结论：**Approve（Squash and merge）**。合并后我会在 `dev` 上重跑连通性（24/24）与审计复核脚本。
```

## 附：第三轮审查的事实依据

| 结论 | 证据 |
|---|---|
| 仅改 2 个文件 | `git show 40e5248 --stat` → `.gitignore`(+3)、`miniprogram/project.config.json`(+25) |
| private 未入库 | `git ls-tree -r --name-only github/feature/frontend -- miniprogram/` → 仅 `project.config.json` |
| gitignore 生效 | 临时工作树 `git check-ignore -v` → `.gitignore:47:...`，exit=0 |
| 可干净合并 | `git merge-base --is-ancestor github/dev github/feature/frontend` → exit=0 |
| 配置字段差异 | Python 对比本地版与入库版：本地独有 `condition/description/libVersion/projectname`；`setting` 键数 35 vs 9 |
| 无敏感信息 | 遍历入库 JSON 顶层键，无 `secret` 字样 |

# 校捷通（XJT Campus）项目审计报告

> **报告生成：2026-09-09 22:47（UTC+8，星期四）**
> 审计日期：2026-09-09
> 审计范围：仓库/Git 健康度、分支与 PR、代码与架构合规、安全、文档与规则、AI/功能进度、自动化工具
> 数据来源：本地仓库实测 + GitHub 远端状态 + 源码抽查

---

## 0. 总体结论（TL;DR）

| 维度 | 评级 | 一句话 |
|---|---|---|
| Git 分支与保护 | 🟢 良好 | `main` 受保护且纯净，`dev` 承载全部最新；近期直合 main 的 PR 均被关闭拦截 |
| 代码/架构合规 | 🟢 良好 | 后端统一走 C++ `jt_db`/`cpp_bridge`、`?` 占位、事务、统一响应基本合规 |
| 后端能力 | 🟢 高完成度 | B1~B5 已入 `dev`（接口+Agent Function Call+RAG 框架） |
| 前端 | 🔴 未开工 | `miniprogram/` 仅规格文档，0 页面代码（最大进度缺口） |
| AI 深度 | 🟡 部分就绪 | RAG/Agent 框架+降级就绪，但真实模型/微调流水线未落地 |
| 安全 | 🔴 需处理 | **数据库密码明文写在 5 处文档并已推 GitHub 公开仓库** |
| 文档与规则 | 🟢 完善 | 协议/分支/归属/规格/任务单/自动同步齐备 |
| 自动化 | 🟡 待推广 | auto-sync 脚本就绪；无 CI、任务计划未全员启用 |

---

## 1. 仓库与 Git 审计

### 1.1 分支健康度（本地=远端）
| 分支 | 提交数 | 状态 |
|---|---|---|
| `main`（稳定，受保护） | 26 | 🟢 纯净：未含任何最近 feature/工具提交 |
| `dev`（集成） | 39 | 🟢 承载全部最新（含 B1~B5、工具、文档） |
| `feature/backend` | 44 | 🟢 队友代码已并入 dev |
| `feature/frontend`/`db`/`db-cpp_driver_src`/`ui`/`docs` | 39 | 🟢 与 dev 同步 |

- 分支保护：GitHub ruleset `team-branch-protect` **仅保护 `main`**（Require PR / Block force push / Restrict deletions），`dev` 放开直推（团队 2026-09-09 决定）——与规则文档一致。
- 完整树模式：feature 分支与 dev 长期同点，优点=任何人切分支都有全套代码；缺点=分支差异难靠 git 体现，依赖"目录归属规范"约束（有 `docs/分支规划与文件归属.md`）。

### 1.2 Pull Request 审计（GitHub）
- **已合并（9）**：#1、#2、#3、#7、#8、#9、#12、#13、#14 —— 早期功能与近期后端 B1~B5(#12/#13)、UI 图(#14) 均进 dev/main 流程。
- **已关闭未合并（9）**：#4、#5、#6（早期“目录隔离”实验）、#11、#15~#19（**均为试图直合 `main` 的 PR，全部被关闭**）——✅ 说明 `main` 保护真实生效，未发生越权合入。
- **存在 open PR：0**。
- ⚠️ 观察：#11、#15~#19 反复出现“feature→main”的建 PR 动作，反映部分成员/建 PR 流程仍习惯指向 `main`，需要流程引导（应一律 `→dev`，或直接走 `dev→main`）。

### 1.3 历史遗留（非阻塞）
- 早期存在 main 直提交、`#8` 的 merge 标题中文乱码（`2c285f3`）；已约定不重写共享历史，从协议生效起新提交均规范。

---

## 2. 代码与架构审计（抽查）

### 2.1 规模（代码量偏小，尚在原型规模）
| 域 | 规模 |
|---|---|
| `backend/app`（Python/FastAPI） | ≈ 2525 行 |
| `db/cpp_driver/src`（C++） | ≈ 828 行 |
| `db/cpp_driver/include` | ≈ 350 行 |
| `db/sql`（42 表种子） | ≈ 875 行 |
| `miniprogram`（前端） | 0 行业务代码 |

### 2.2 合规抽查结论
- ✅ 数据访问统一经 `app/db/cpp_bridge.py`（`import jt_db`），未见 Python 直连 MySQL；
- ✅ SQL 使用 `?` 占位符（防注入）；事务用 `with cpp_bridge.begin():`；
- ✅ 统一响应 `{code,message,data}` + `BizError`；JWT 鉴权 `get_current_user`；
- ✅ C++ 层：连接池/RAII/预处理语句/6 个 DAO，测试 `test_all_dao.py` 覆盖事务回滚；
- ✅ `.gitignore` 完善，无编译产物/缓存被跟踪；
- ⚠️ 无自动化测试接入 CI（后端测试脚本存在但未在 GitHub Actions 跑）；无静态检查（lint）。

---

## 3. 安全审计 🔴（优先处理）

| 级别 | 问题 | 位置 | 建议 |
|---|---|---|---|
| 🔴 高 | **MySQL 密码 `jhq000000` 明文写进文档并推送到 GitHub 公开仓库** | `docs/datagrip.md`、`docs/DEVELOPMENT.md`(多处)、`docs/四成员任务单.md`、`.github/copilot-instructions.md` | 1) 全部替换为 `***`/`${XJT_DB_PASSWORD}`；2) 若仓库为 Public，考虑把该密码视为已泄露并轮换；3) 文档仅保留占位符 |
| 🟡 中 | `core/config.py` 中 `jwt_secret` 默认 `"xjt-dev-secret-change-me"` | `backend/app/core/config.py:24` | 生产必须经环境变量注入强随机密钥；开发默认值可接受但需注释 |
| 🟢 低 | 微信 `appid/secret` 留空（走模拟） | `config.py` | 接入前确保经环境变量注入，勿入库 |
| 🟢 低 | git 凭据/代理 | — | 无 token 入库；github 走本地代理 127.0.0.1:7897 |

---

## 4. 文档与规则审计

- ✅ 规则体系齐备：`团队Git合作协议.md`（权威，含保护现状+自动同步规范）、`BRANCH_STRATEGY.md`（速查）、`分支规划与文件归属.md`、`api.md`（接口契约）、`前端页面规格.md`（含跳转图 §0A）、`四成员任务单.md`、`DEVELOPMENT.md`、PR 模板、Copilot 指令。
- ✅ Copilot 指令已含 Git 协作规范（commit 类型等）与架构铁律。
- ⚠️ 多份早期文档与最新规则曾不一致（如保护策略），已随 2026-09-09 决定对齐；**四成员任务单 B1 等仍含明文密码**（见安全节）。
- ⚠️ 待补文档：需求说明书、ER 图、AI 模型训练报告（DEVELOPMENT 里程碑仍空）。

---

## 5. 功能与 AI 进度审计

| 项 | 状态 | 备注 |
|---|---|---|
| 数据库 42 表 / C++ 数据层 | ✅ | 已完成+测试 |
| 后端 11 模块接口 | ✅ | 已实测，入 dev |
| B1~B5（缺口接口/Agent FC） | ✅ | #12/#13 合入 dev；Agent Function Call 执行器已实现 |
| RAG 框架 | 🟡 | 框架+降级就绪；需 Ollama(bge-m3)+喂知识才能真检索 |
| 真实模型接入 | 🔴 | Ollama 对话模型未接入，仍走降级 |
| 模型微调流水线 | 🔴 | `ai/finetune` 仍空（成员3 待做 C1~C5） |
| **前端页面** | 🔴 | `miniprogram` 仅规格/README，0 页面（F1~F9 未开始） |
| 部署（Docker/HTTPS） | 🔴 | 未开始 |
| 里程碑 | 🟡 | 2026-08-24 各项 ✅；其后 AI/前端/部署未更新 |

> 关键路径判断：**前端（成员1）仍是最大缺口与进度主线**；AI 真实模型/微调决定“智能成色”上限；两者是后续答辩成败关键。

---

## 6. 自动化与协作工具审计

- ✅ `scripts/auto-sync`（自动拉取主仓库脚本）已入库 `dev`（可执行位 100755、支持 `XJT_REMOTE_NAME` 环境变量覆盖、LF 保护 `.gitattributes`）。
- ⚠️ 协作者尚未普遍注册 30 分钟调度；本机未启用计划任务。
- ❌ **无 CI**：无 GitHub Actions 做“push/PR 自动测试/构建校验”，合并仅靠人工 review。

---

## 7. 风险登记与建议

### P0（立即处理）
1. **清除文档明文数据库密码**并轮换（若仓库 Public）；`.github/copilot-instructions.md`、`docs/DEVELOPMENT.md`、`docs/datagrip.md`、`docs/四成员任务单.md` 全改占位符。
2. **前端动工**：按 `前端页面规格.md` + `前端跳转文件1.0.drawio` 先做 F1~F3（工程+登录+首页），尽早出演示闭环。

### P1（本周内）
3. 把 auto-sync 推广给全员（每人在自己机器配 30 分钟调度，目标 `dev`）；给流程建 PR 一律 `→dev`。
4. 接入真实模型与 RAG 全量索引（成员2 依赖 Ollama）+ 微调流水线（成员3）。
5. 补需求说明书 / ER 图（成员4），供评审/结题。

### P2（持续）
6. 引入最小 CI（GitHub Actions：语法检查 + `test_all_dao.py`/后端冒烟，PR 自动跑）。
7. `jwt_secret`/微信密钥明确环境变量注入策略。
8. 修复 #8 merge 乱码类历史遗留（可选，不重写历史前提下仅作记录）。

---

## 8. 结论

项目**地基扎实、规则与工具建设良好**（Git 治理、文档体系、数据层、后端接口均到位），但**当前处于"后端完成、前端空白、AI 待真"的阶段**。当务之急：① 安全清理文档密码；② 前端立即开工形成演示闭环；③ AI 接真实模型/微调。配合已就绪的自动同步与分支保护，团队可按规范进入高强度并行开发期。

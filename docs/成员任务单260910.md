# 校捷通 · 四人任务分工（2026-09-10 版）

> 生成日期：**2026-09-10（周四）**。用法：每位成员只看自己那段，做完一项勾一项；产出 = **能跑通可演示**。
> 阶段定位：后端 **B1~B5 已完成并入 `dev`** ✅ → 本阶段攻坚 **前端页面** 与 **AI 真实模型/微调**。
> 规则：Git 协作以 `docs/团队Git合作协议.md` 为准（`main` 禁推、PR 一律 → `dev`）；每天用 `bash scripts/auto-sync/sync.sh dev` 自动同步主仓库。
> ⚠️ 今日 P0：**清理文档中的明文数据库密码**（成员4 D0）；成员3 为 C++/微调/数据库负责人。

---

## 👤 成员1 · 前端小程序（分支 `feature/frontend`，只改 `miniprogram/`）

**今日目标：完成 F1~F3，跑通「登录 → 首页 → 跳转」最小闭环，并把 F1 成果提交到 `feature/frontend` 后发 PR → `dev`。**

> 蓝本：`miniprogram/前端页面规格.md`（含 §0A 前端跳转图 `ui/前端跳转文件1.0.drawio`）；接口见 `docs/api.md`，后端已全部实现。

| 任务 | 产出 / 验收 | 今日 |
|---|---|---|
| F1 工程三件套 | `app.json`(注册页+5个TabBar) + `app.js`(token检查) + `services/request.js`(带token/统一错误/SSE解析) | 🔴 必达 |
| F2 登录页 `pages/auth/login` | 点【微信一键登录】→ `POST /auth/wechat-login` → 存token跳首页 | 🔴 必达 |
| F3 首页 `pages/index` | 10个宫格入口能跳转；快捷指令条【查空教室/查校历】调通 | 🔴 必达 |
| F4 AI对话页 `pages/chat` | `POST /chat/send` SSE 逐字渲染；【停止】【+新会话】【历史】 | 🟡 明日 |
| F5 空教室 / F6 座位预约 | 见规格书 §6（真数据 + code 3001 冲突提示） | 🟡 排期 |
| F7 论坛 / F8 我的 | 列表+发帖+详情点赞评论；资料/预约入口 | 🟡 排期 |
| F9（进阶）业务页 | 二手(带AI描述)/兼职/地图/外卖 —— 规格书 §7~§10 | ⚪ 后续 |

**提交要求**：F1 完成即先提交（别只留本地），每个功能一次小提交，PR 目标 `dev`；信息用 `feat(frontend): ...`。

---

## 👤 成员2 · Python 后端 + AI 工程（分支 `feature/backend`）

**B1~B5 已完成 ✅（#12/#13 已合入 dev）**。今日目标：**让对话走真实模型、RAG 真检索、Agent 端到端可用。**

| 任务 | 产出 / 验收 | 今日 |
|---|---|---|
| B3' RAG 全量索引 | 装 Ollama + 拉 `bge-m3`；`cd backend && python ../ai/rag/build_index.py --force`；`retrieve.py "图书馆几点关门"` 能查到知识 | 🔴 必达 |
| B4' 对话接真模型 | Ollama 拉对话模型（qwen2.5:7b 或成员3 微调产物）；`chat/send` 返回真实回答（去掉降级占位），SSE 正常 | 🔴 必达 |
| B5' Agent 端到端验证 | 一句话触发真实预约/提醒/发布（Function Call 主路径），非降级 | 🔴 必达 |
| B2' 回归与答疑 | 收藏/我的帖子/我的发布三接口回归；联调答疑；补 `docs/api.md` 用例 | 🟡 |
| B6（进阶） | 内容审核接模型、二手 AI 描述/定价、通知精准推荐 | ⚪ 后续 |

**依赖**：Ollama 就绪；无 GPU 时先用 7B 量化跑通，微调产物再替换。

---

## 👤 成员3 · C++ 数据层 + 模型微调 + 数据库（分支 `feature/db-cpp_driver_src`）

**C++ 数据层已完成 ✅**。今日目标：**立起微调流水线骨架并跑出第一份训练数据。**

| 任务 | 产出 / 验收 | 今日 |
|---|---|---|
| C1 微调流水线骨架 | `ai/finetune/`：`build_dataset.py` + `train.py`(QLoRA，PEFT+bitsandbytes) + `eval.py` 可跑 | 🔴 必达 |
| C2 第一份训练数据 | 导出 ≥500 条格式正确的 `train.jsonl`（论坛/知识库文本转指令） | 🔴 必达 |
| C3 知识库扩料 | `knowledge_doc` 入 ≥20 篇（图书馆规则/奖学金/校医院/校历），配合成员4 | 🟡 |
| C6 性能素材 | C++ 层 vs 直连的性能压测数据（答辩素材） | 🟡 |
| C4/C5 试跑+GGUF | QLoRA 小轮次试跑 → 转 GGUF 交成员2 载入 | ⚪ 后续 |

**依赖**：C2/C3 数据与成员4 协作；C4 需少量显存（QLoRA 4bit，学生机/Colab 亦可）。

---

## 👤 成员4 · 数据库 + 文档 + 数据（分支 `docs` / `feature/db`）

**今日目标：消除安全隐患 + 给全队"喂料 + 留档"。**

| 任务 | 产出 / 验收 | 今日 |
|---|---|---|
| **D0（P0）安全清理** | 将 `docs/datagrip.md`、`docs/DEVELOPMENT.md`、`docs/四成员任务单.md`、`.github/copilot-instructions.md` 中明文密码 `jhq000000` 全部替换为 `***`/`${XJT_DB_PASSWORD}`；提交 `docs` 分支并发 PR → `dev`；提醒全员轮换密码 | 🔴 必达 |
| D1 训练语料 | 论坛/对话/知识库文本 **脱敏**（去学号/手机号/姓名）→ 入 `train_corpus`，供 C2 | 🟡 |
| D3 需求说明书 | `docs/需求说明书.md` 初稿（背景/功能清单/用例） | 🟡 |
| D4 数据库 ER 图 | DataGrip（`docs/datagrip.md`）反向导出 → `docs/db/ER图` | 🟡 |
| D5 api.md 维护 | 接口改动先登记 `docs/api.md` 再实现 | 🟡 持续 |
| D6 auto-sync 推广 | 收集各成员是否已启用 30 分钟自动同步（`scripts/auto-sync/README.md`） | 🟡 |

---

## 今日协作节奏（2026-09-10）

1. **每天**：群里一句话进度/卡点（例："F3 首页宫格跳转已通"）。
2. **今日 18:00 前**：成员1 完成 F1~F3 并发 PR；成员4 完成 D0 安全清理；成员2 完成 B3'/B4'。
3. **周五（09-11）**：各分支 PR 合 `dev`，跑一次「演示闭环」，README 里程碑表打勾。
4. **改接口**：先改 `docs/api.md` 再动代码；前后端以它为准。
5. **同步**：每人启用 auto-sync（`bash scripts/auto-sync/sync.sh dev`），本地始终跟 `dev` 一致。
6. **验收标准**：任何"完成"= 能演示真数据，不是代码写完。

---

## 补充批次：审计修复（成员3 · 2026-09-10 晚 · 已合入 `dev`）

> 依据 `docs/项目审计报告260910序2-技术专项.md`（46 项风险：P0×11 / P1×18 / P2×17）。
> 全部改动已推送 `dev`，每个批次都带**可复现的验证脚本**（`backend/tests/`）。

### 第 1 批 · P0 × 11（commit `caf17fa`，PR 见 `fix/p0-hardening`）

| 维度 | 编号 | 一句话 |
|---|---|---|
| 权限 | SEC-01 | 生产环境强制 ≥32B JWT 密钥，仍用默认值则**拒绝启动** |
| 权限 | SEC-02 | 微信登录 mock 降级仅限非生产；生产未配凭据直接拒启 |
| 权限 | SEC-03 | 聊天消息读取增加会话归属校验（原可 IDOR 读他人对话） |
| 权限 | SEC-04 | 向指定会话写入时校验归属（原可注入他人历史，造成提示注入外泄） |
| 权限 | SEC-05 | 下单契约改为 `{item_id, remark}`，卖家/金额**服务端反查**（原可伪造价格、改他人商品状态） |
| 权限 | SEC-06 | `user_dao` SELECT 补齐 `status/is_deleted` 等列 —— 原禁用账号校验是**死代码** |
| 并发 | CON-01 | 连接池 16 → 可配置 4~32（原池上限 < 线程池 40） |
| 并发 | CON-02 | 事务按 owner 线程释放，杜绝跨线程误清理 |
| 事务 | TXN-01 | 座位/房间冲突检查改 `SELECT ... FOR UPDATE`（原快照读可双订） |
| 事务 | TXN-02 | 二手订单原子抢占 `UPDATE ... WHERE status=0`，防超卖 |
| 缓存 | CAC-01 | RAG 关键词检索改 `to_thread` + 信号量，避免 AI 故障升级为全站雪崩 |

**验证**：`python backend/tests/verify_p0_authz.py` → **10/10**（真实 HTTP 越权实测）。

### 第 2 批 · P1 首批（commit `c651edc` / `809bb83` / `4e9f02e`）

| 编号 | 一句话 | 验证脚本 |
|---|---|---|
| SEC-10 | 新增滑动窗口限流中间件（登录 10 次/分，超限 429 + `code=1010`） | `verify_p1_ratelimit.py` 5/5 |
| SEC-13 | CORS 修复：通配来源 + `allow_credentials=True` 的组合会让 Starlette 回显任意 Origin | 同上 |
| SEC-11 | 上传改为**魔数校验**真实类型（原只看客户端 `Content-Type`，可传伪装 HTML）+ 静态目录加 `nosniff`/`CSP sandbox` | `verify_p1_upload.py` 5/5 |
| CAC-03 | 浏览量改内存聚合 + 周期批量落库，消除热门帖行锁热点（写放大 20:1 → 1:1） | `verify_p1_viewcounter.py` 4/4 |

### 交接说明（重要）

1. **接口契约已变更**：`POST /secondhand/orders` 请求体为 `{item_id, remark}`，
   响应新增 `amount`。已同步 `docs/api.md`（v1.4）。**成员1 请按新契约改小程序端。**
2. **需本地重编译 C++**：`SEC-06` 改了 `user_dao.cpp`。执行
   `powershell -ExecutionPolicy Bypass -File db/cpp_driver/scripts/build.ps1`
   生成 `backend/app/db/native/jt_db.pyd`（Python 3.14 ABI）。
3. **成员1 阻塞已解除**：`dev` 已含 `PyJWT` 依赖与全部 P0 修复，`/health` 返回
   `{"db":"ok","cpp_ext":true}`，F2 可做**真实登录**联调（此前仅 Mock 验证）。
4. **下一批 P1**：SEC-07/08（登出与 refresh 轮换，需改 `user` 表加 `token_version`，
   涉及全员同步 SQL + C++ 重编译）、SEC-09/14、CAC-02/05/06/07。

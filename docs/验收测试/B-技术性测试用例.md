# 第一阶段验收 · B 分册｜技术性测试用例

> **配套**：`00-第一阶段验收测试方案.md`（准入 §2、判定 §5、豁免 §7）
> **执行人**：成员3（主）+ 成员2 / 成员1 配合
> **判定原则**：**以脚本输出/SQL 结果为准**，不靠肉眼观察
> **覆盖**：B-1 契约 → B-2 鉴权 → B-3 错误码 → B-4 并发 → B-5 性能 → B-6 AI → B-7 数据层 → B-8 异步 → B-9 安全 → B-10 工程 → B-11 部署预检

---

## 执行约定

```powershell
# 统一使用 miniconda Python（项目 .venv 无 httpx）
$PY = 'E:\miniconda3\python.exe'
# 环境变量（每个新终端都要设）
$env:XJT_DB_PASSWORD='jhq000000'; $env:XJT_DB_PORT='3307'
# 工作目录（AI 评估必须在此目录，因为 rag_vector_dir 是相对路径）
cd d:\xiaojietongproject\xiaojietong-project\backend
```

**通用判据**：`{"code":0,"message":"ok","data":{...}}`；`BizError` 返回 **HTTP 400**（不是 200）。

---

## B-1 · 接口契约一致性（必测，门槛 100%）

**目的**：验证 C++ DAO ↔ 后端路由 ↔ 文档三方一致，且返回字段与 `docs/api.md` 相符。

### B-1.1 路由覆盖

| 编号 | 测试点 | 方法 | 通过判据 |
|---|---|---|---|
| B-1-1 | 前端调用的接口后端全部存在 | `$PY tools\audit_frontend_backend.py` | **无"前端调用但后端缺失"项**（历史曾有 `GET /secondhand/items/{id}` 缺失，PR42 已补） |
| B-1-2 | 后端接口全部可连通 | `$PY tools\e2e_connectivity.py` | **全部非 404**（基线 23/23） |
| B-1-3 | Swagger 可访问 | `http://127.0.0.1:8000/docs` | 页面打开，接口数 ≈ 79 |
| B-1-4 | 路由无遗漏注册 | `$PY tools\audit_frontend_backend.py` 输出后端路由总数 | 与 `docs/api.md` §1 总览逐模块对应 |

### B-1.2 字段级契约（逐模块抽检，每模块 ≥ 2 接口）

| 编号 | 模块 | 检查点 |
|---|---|---|
| B-1-5 | 认证 | `POST /auth/wechat-login` 返回 `token`/`refresh_token`/`user{id,openid,nickname,avatar,role,grade,major,campus,is_new}` |
| B-1-6 | 用户 | `GET /user/me` 字段与 login 的 `user` **完全一致**（无多无少） |
| B-1-7 | AI | `GET /chat/conversations` 返回 `items` + 分页字段 |
| B-1-8 | Agent | `GET /agent/tasks` 每条含 `task_type/status/params/result/error_msg`；`status` 文案与数据库枚举一致 |
| B-1-9 | 图书馆 | `GET /library/rooms/{id}/seats` 座位含行列坐标与状态 |
| B-1-10 | 二手 | `GET /secondhand/items` 含 `images`（**数组，不是字符串**）、`price`、`condition_level` |
| B-1-11 | 兼职 | `GET /jobs/{id}` 含 `trust` 相关信息 |
| B-1-12 | 论坛 | `GET /topics/{id}` 含 `comments` 数组、`audit_status` |
| B-1-13 | 地图 | `POST /map/navigate` 含**折线点数组** + `straight_distance` |
| B-1-14 | 生活 | `GET /life/orders/{id}` 含**收货信息字段**（⚠️ 历史丢字段风险） |
| B-1-15 | 管理 | `GET /admin/metrics` 数值字段齐全 |

> **真实契约比对工具**：`$PY tools\pr39_field_probe.py`（真实调用接口打印返回字段形状，做字段级比对，无副作用）

### B-1.3 分页语义（⚠️ 已知缺陷）

| 编号 | 测试点 | 方法 | 通过判据 | 当前预期 |
|---|---|---|---|---|
| B-1-16 | 分页 `total` 是**总条数** | 请求 `?page=1&size=3` 与 `?page=2&size=3` | 两次 `total` **相同且等于总数** | ❌ **已知失败**（实测 9 处返回"本页条数"） |
| B-1-17 | `page` 回显正确 | 同上 | `page` 与请求一致 | ✅ |
| B-1-18 | 越界页 | `?page=999` | 返回空 `items`，**不报错** | ✅ |
| B-1-19 | `size` 边界 | `size=0` / `size=101` / `size=abc` | 按契约返回参数错误码（1001），非 500 | ⚠️ 待验 |

---

## B-2 · 鉴权与越权（必测，门槛 100%）

| 编号 | 测试点 | 方法 | 通过判据 |
|---|---|---|---|
| B-2-1 | 无 token 访问受保护接口 | 对 12 个模块各取 1 接口，不带 `Authorization` | **全部 401**，code=2001 |
| B-2-2 | 伪造 token | 用 `alg=none` 或错误签名 | **401**，不返回数据 |
| B-2-3 | 过期 token | 手工构造过期 token | code=2002 |
| B-2-4 | **水平越权**：读他人会话 | 甲 token 读乙的 `GET /chat/conversations/{id}/messages` | **拒绝**（历史 IDOR 已修） |
| B-2-5 | **水平越权**：取消他人预约 | 甲 token 取消乙的预约 | **拒绝** |
| B-2-6 | **水平越权**：改他人商品状态 | 甲 token 改乙的 `PUT /secondhand/items/{id}/status` | **拒绝** |
| B-2-7 | **垂直越权**：学生调管理接口 | 学生 token 请求 `/admin/metrics`、`/admin/forum/audit`、`/admin/knowledge/index` | **全部拒绝**，code=2003 |
| B-2-8 | 禁用账号失效即时生效 | SQL 置 `user.status=1` → 用旧 token 请求 | **拒绝**（code=2003） |
| B-2-9 | 一键脚本 | `$PY backend\tests\verify_p0_authz.py` | **10/10** |

> ⚠️ **注意事项**：`deps.py` 里判断 `user.get("status") == "1"` 是**字符串比较**（C++ 层把类型转成了字符串）。测试时若发现"禁用后仍能访问"，记录为缺陷。

---

## B-3 · 参数校验与错误码契约

| 编号 | 测试点 | 方法 | 通过判据 | 当前预期 |
|---|---|---|---|---|
| B-3-1 | 缺必填参数 | `POST /topics` 不带 title | code=1001（HTTP 400） | ✅ |
| B-3-2 | 类型错误 | `POST /library/reservations` 传字符串 id | code=1001 | ✅ |
| B-3-3 | 空 body | 同上，body 为空 | **契约错误码** | ❌ **已知失败**（返回 FastAPI 422 `{"detail"}`，缺 RequestValidationError handler） |
| B-3-4 | 非法枚举 | `?period=abc` | code=1001 | ❌ **已知失败**（返回 500） |
| B-3-5 | 业务冲突码 | 重复预约同一座位 | code=**3001** | ✅ |
| B-3-6 | 审核拒绝码 | 发含「代考」的帖子 | code=**3003** | ✅ |
| B-3-7 | 限流码 | 超频请求 | code=**1010** + HTTP 429 | ✅ |
| B-3-8 | 服务端错误码 | 停 Ollama 后请求 AI | code=5002（或优雅降级） | 待验 |
| B-3-9 | 不存在的资源 | `GET /topics/99999999` | code=1001/3001，**非 500** | 待验 |

**工具**：`$PY tools\audit_probe_260912.py`（含分页语义、错误码契约探针）

---

## B-4 · 并发与事务（必测，门槛 100%）

| 编号 | 测试点 | 方法 | 通过判据 |
|---|---|---|---|
| B-4-1 | **座位不超卖** | 10 个并发请求抢同 1 个座位 | **恰好 1 个成功**，其余 3001 |
| B-4-2 | **订单不超卖** | 并发下单同一商品 | 不产生负库存 / 不产生重复订单 |
| B-4-3 | **重复投递** | 并发投递同一岗位 | 只 1 条记录（唯一约束或原子判断） |
| B-4-4 | **点赞幂等** | 并发点赞同 1 帖 | 计数正确（不为负、不大幅超额） |
| B-4-5 | 事务回滚 | 构造中途失败的下单（如菜品不存在） | **整体回滚**，无半成品数据 |
| B-4-6 | 连接池不泄漏 | 并发 200 请求后看 health | `idle` 恢复、`active` 归零 |
| B-4-7 | 浏览量聚合 | `$PY backend\tests\verify_p1_viewcounter.py` | **4/4** |

**工具**：`db/cpp_driver/test/bench_concurrent.py`（历史并发脚本）；如需可写一次性压测脚本。

> ⚠️ **已知结构性风险**：大量路由是 `async def` 内部调用**同步**的 C++ DB → 事件循环仍是单点。
> 并发测试若出现"请求排队但 CPU 不高"，记为**已知性能风险**（`CON-xx`），不必当新缺陷。

---

## B-5 · 缓存与性能

| 编号 | 测试点 | 方法 | 通过判据 |
|---|---|---|---|
| B-5-1 | RAG 结果缓存命中 | 同问句连发 2 次，对比耗时 | 第 2 次显著更快（TTL 60s） |
| B-5-2 | **single-flight 合并** | 并发 10 个**相同**问题 | 只触发 **1 次**上游检索/模型调用 |
| B-5-3 | 单飞脚本 | `$PY -m pytest backend\tests\test_rag_singleflight.py -q` | 通过 |
| B-5-4 | 索引重建空窗 | 重建索引期间并发查询 | **不返回空**（历史"空窗"已修） |
| B-5-5 | 索引重建脚本 | `$PY -m pytest backend\tests\test_index_rebuild.py -q` | 通过 |
| B-5-6 | 读写锁 | `$PY -m pytest backend\tests\test_rwlock.py -q` | 通过 |
| B-5-7 | 列表接口延迟 | 对 8 个列表接口各压 50 次 | **p95 < 500ms**（本地） |
| B-5-8 | 缓存无界增长 | 灌 600 个不同问句 | 缓存条目 ≤ 512（有上限且淘汰） |
| B-5-9 | 空结果短 TTL | 查询无结果问句后立即再查 | 15s 内不重复打库（空结果短 TTL 生效） |

> **性能基线参考**：`docs/perf-benchmark.md`（历史压测结论）。
> ⚠️ **已知缺陷**：`(? = '' OR col = ?)` 写法让分类索引失效（4 个 DAO）→ 数据量大时列表会慢，记为 `CAC-xx` 遗留。

---

## B-6 · AI 能力指标（一阶段核心，重点）

### B-6.1 校园 RAG（C14 已交付）

| 编号 | 测试点 | 方法 | 通过判据 | 当前基线 |
|---|---|---|---|---|
| B-6-1 | 检索命中率 | `cd backend; $PY -X utf8 ..\ai\eval\rag_bench.py --out ..\ai\eval\out\rag_baseline.json` | **hit@3 ≥ 80%** | ✅ **100%** |
| B-6-2 | hit@1 | 同上 | 记录值 | 92.0% |
| B-6-3 | MRR@5 | 同上 | 记录值 | 0.953 |
| B-6-4 | 空结果率 | 同上 | 越低越好 | 0% |
| B-6-5 | **负样本误命中率** | 同上（含 2 条负样本） | **应 < 100%** | ❌ **100%**（阈值 0.35 偏低，C20 待做） |
| B-6-6 | 延迟 | 同上 | p50 记录值 | 43.5ms（平均 692ms 含首次加载） |
| B-6-7 | 索引完整 | `SELECT COUNT(*) FROM knowledge_chunk` | chunk > 0 | ✅ 27 篇全 ok |
| B-6-8 | 门禁开关 | `--threshold 0.9` | **exit code = 1**（CI 门禁可用） | ✅ |

> ⚠️ **运行目录**：`rag_vector_dir`（默认 `data/rag`）是**相对路径** → 建索引与评估**必须在同一目录**（推荐 `backend/`），否则表现为"检索全空"。

### B-6.2 内容审核（B6）

| 编号 | 测试点 | 方法 | 通过判据 |
|---|---|---|---|
| B-6-9 | 违规词拦截 | 发帖含「代考」 | code=**3003**，帖子**不入库/不可见** |
| B-6-10 | 待审词转人工 | 发帖含「私聊」 | `audit_status=0` 待审 |
| B-6-11 | 干净内容秒过 | 正常帖子 | 立即 `audit_status=1`，他人可见 |
| B-6-12 | 留痕 | `SELECT * FROM audit_log` | 有记录（规则 0ms / 模型 0.6~3s） |
| B-6-13 | ⚠️ 读路径过滤 | 待审帖 → 他人 `GET /topics` | **不应出现** | ❌ **已知失败**（`DATA-01`） |
| B-6-14 | ⚠️ Agent 发布过审 | Agent 工具发布二手 | **应走审核** | ❌ **已知失败**（`DATA-02`） |

### B-6.3 Agent 三级链路（B7）

| 编号 | 测试点 | 方法 | 通过判据 |
|---|---|---|---|
| B-6-15 | 模型可用 → Function Call | 正常提问 | `result.source="model"` |
| B-6-16 | 模型不可用 → 规则兜底真写库 | 停 Ollama 后提问 | `source="rule"` 且**数据库确有记录** |
| B-6-17 | 都未识别 → 明确失败 | 无意义指令 | `status=3` + `error_msg`，**绝不假成功** |
| B-6-18 | 相对时间正确 | 「10分钟后提醒我」 | `remind_at` 与当前时间差 ≈ 10 分钟（基准日期注入） |

### B-6.4 微调模型（C1~C5）

| 编号 | 测试点 | 方法 | 通过判据 |
|---|---|---|---|
| B-6-19 | 模型已装载 | `ollama list` | 含 `xjt-3b` |
| B-6-20 | 微调效果 | `$PY ai\finetune\eval_compare.py` | 微调版优于基座（见 `ai/finetune/评测报告.md`） |
| B-6-21 | 模型可对话 | 通过后端提问 | 中文回答通顺、校园语境正确 |

---

## B-7 · 数据层（C++ jt_db）

| 编号 | 测试点 | 方法 | 通过判据 | 当前基线 |
|---|---|---|---|---|
| B-7-1 | DAO 断言 | `cd db\cpp_driver\test; $PY test_all_dao.py` | **15/15**（含事务回滚） | ✅ |
| B-7-2 | 连接池初始化 | health/detail | `cpp_ext=true`、池参数正确 | ✅ |
| B-7-3 | 事务隔离 | 并发事务测试 | RR 隔离级行为正确 | ✅ |
| B-7-4 | SQL 注入 | 对 3 处 `sql +=` 的开关路径传恶意串 | 无注入（LIMIT/值均 `?` 绑定） | ✅ |
| B-7-5 | 表结构完整 | `SHOW TABLES` | **45 张表** | ✅ |
| B-7-6 | 种子数据 | 逐表 `COUNT(*)` | 关键表非空 | 待验 |
| B-7-7 | NULL/类型处理 | 查询含 NULL 字段的记录 | ⚠️ 已知：NULL→`""`、`py::str(v)` 使**数字变字符串** → 若前端做数值比较会出错，记为 `DATA-xx` 遗留 | ⚠️ |
| B-7-8 | `SELECT LAST_INSERT_ID()` | 插入后取 ID | 与 INSERT **同连接**才有效（跨连接返回 0） | ⚠️ 注意 |

---

## B-8 · 异步任务（B15 · Celery）

| 编号 | 测试点 | 方法 | 通过判据 | 本轮是否测 |
|---|---|---|---|---|
| B-8-1 | 默认降级（eager） | 不设 `XJT_CELERY_ENABLED`（默认 false） | `/agent/tasks` 返回**与旧版一致**（含执行结果，`status=2/3`） | ✅ 测 |
| B-8-2 | eager 下索引重建同步返回 | `POST /admin/knowledge/index` | 返回 `{total, ok, failed, details}` | ✅ 测 |
| B-8-3 | health 暴露状态 | `GET /health/detail` | 含 `celery.mode="eager(sync)"` | ✅ 测 |
| B-8-4 | **异常转契约** | 临时让 C++ 扩展不可用后调 `/agent/tasks` | 应返回**契约错误** | ❌ **已知问题**：`task_eager_propagates=True` 会让 `.delay()` 直抛 → **HTTP 500** |
| B-8-5 | 真异步（需 Redis） | 启动 Redis + worker（Windows `-P solo`）→ 设 `XJT_CELERY_ENABLED=true` | 返回 `status=0` + `celery_task_id`，轮询得终态 | ⛔ **本轮不测**（需 Redis + 前端轮询未实现） |
| B-8-6 | 异步索引状态接口 | `GET /admin/knowledge/index-status/{id}` | 异步下能查进度 | ⛔ 本轮不测 |
| B-8-7 | worker 连接池 | worker 内任务执行 | 依赖 `_ensure_db()` 惰性初始化（已实现） | ⛔ 需真异步环境 |

> **本轮结论应标注**：B15 为"**已交付但默认关闭**"，异步路径**未纳入本次验收**（需 Redis 环境 + 前端轮询实现）。

---

## B-9 · 安全专项

| 编号 | 测试点 | 方法 | 通过判据 | 当前预期 |
|---|---|---|---|---|
| B-9-1 | 限流生效（同 IP） | 同 IP 连续 12 次登录 | 第 11 次起 429（code 1010） | ✅ |
| B-9-2 | **限流防绕过** | 每次换伪造 `X-Forwarded-For` 连发 15 次 | **应被拦截** | ❌ **已知失败**（15/15 全 200，`SEC-17` P0） |
| B-9-3 | 一键限流脚本 | `$PY backend\tests\verify_p1_ratelimit.py` | **5/5** | ✅ ⚠️ 跑前**重启后端**清计数 |
| B-9-4 | CORS 白名单 | 从非白名单 Origin 请求 | 被拒 / 不返回 `*`（`cors_origins` 默认 `*` 需注意） | ⚠️ |
| B-9-5 | 上传魔数校验 | 伪造扩展名的非图片文件 | code=1001 拒绝 | ✅ |
| B-9-6 | 上传一键脚本 | `$PY backend\tests\verify_p1_upload.py` | **5/5** | ✅ |
| B-9-7 | 签名 URL 直连 | 带签名 200 / 无签名 403 / 篡改 403 | 符合 | ✅ |
| B-9-8 | ⚠️ **业务接口图片可访问** | 取列表/详情/收藏返回的 `images`/`avatar` 直接访问 | **应可访问** | ❌ **已知失败**（P1 签名闭环缺口，返回裸路径 → 403） |
| B-9-9 | JWT 密钥强度 | 检查 `config.py` 默认密钥 | 生产必须外部注入 | ❌ **已知失败**（`SEC-18`，仓库内置默认值） |
| B-9-10 | 生产模式硬校验 | `XJT_ENV=prod` 启动（不配密钥） | **启动失败** | ✅ |
| B-9-11 | 登出失效 | 登出后用旧 token 请求 | **应失效** | ❌ 已知未修（`SEC-07`，需改表） |
| B-9-12 | refresh 轮换 | refresh 后用旧 refresh_token | 应失效 | ❌ 已知未修（`SEC-08`） |
| B-9-13 | 路径穿越 | 请求 `/static/uploads/../../etc/passwd` | 拒绝 | 待验 |
| B-9-14 | 静态资源安全头 | `static_safety` 中间件 | 生效 | 待验 |
| B-9-15 | 敏感信息不落响应 | 检查响应中是否含密码/密钥 | 无 | 待验 |
| B-9-16 | 审计复核脚本 | `$PY tools\audit_recheck_260912.py` | 输出三态判定，与 §7 豁免清单一致 | 见输出 |

> ⚠️ **`SEC-17`（XFF 绕过）与 `SEC-18`（默认密钥）均为 P0，虽在 §7.2 豁免，但建议答辩前修掉** —— 评委只要看一眼 `ratelimit.py` 就能发现。

---

## B-10 · 工程化

| 编号 | 测试点 | 方法 | 通过判据 | 当前状态 |
|---|---|---|---|---|
| B-10-1 | 后端单测 | `cd backend; $PY -m pytest tests -q` | 全绿 | ✅ **7 passed / 0.66s** |
| B-10-2 | 语法检查 | `$PY -m compileall backend\app` | exit=0 | ✅ |
| B-10-3 | 依赖可重装 | 按 `requirements.txt` 新建 env 安装 | 成功 | ⚠️ `pymysql` 被脚本使用但**未声明** |
| B-10-4 | 未使用依赖 | 检查 `SQLAlchemy/alembic` | 已加注释说明 | ⚠️ 仍占位 |
| B-10-5 | `.env.example` 完整 | 对照 `config.py` 全部 `XJT_*` | 无遗漏 | ✅（32 项） |
| B-10-6 | **CI 存在** | 看 `.github/workflows/` | 有工作流 | ❌ **无 CI**（`ENG-01`） |
| B-10-7 | 一键跑全部脚本 | 按 §9 清单依次执行 | 全部可跑通 | 待验 |
| B-10-8 | 文档与实现一致 | 抽查 `api.md` 变更记录 vs 实际路由 | 无漂移 | ⚠️ 历史曾漂移 |

---

## B-11 · 部署预检（不做上线，只查"上线前必须知道的坑"）

| 编号 | 测试点 | 方法 | 通过判据 | 当前状态 |
|---|---|---|---|---|
| B-11-1 | `BASE_URL` 可配置 | 检查 `services/request.js` | **不写死 127.0.0.1** | ❌ `FRONT-01` P0 |
| B-11-2 | 微信合法域名要求 | 核对小程序后台 | 生产必须 **https** | ⚠️ 未配置 |
| B-11-3 | nginx 上传体积 | 检查方案文档 | `client_max_body_size` ≥ 5MB | ❌ **缺失**（默认 1MB vs 后端 5MB） |
| B-11-4 | 容器化 | 找 Dockerfile | 存在 | ❌ 无 |
| B-11-5 | 部署脚本 | 看 `deploy/` | 有可执行内容 | ❌ 只有 README |
| B-11-6 | 健康探针路径 | 请求 `/health` 与 `/api/v1/health` | 探针要打**后者**（前者 404） | ⚠️ 易踩坑 |
| B-11-7 | 数据备份方案 | 有无备份脚本 | 有 | ❌ 待补 |
| B-11-8 | SQL 执行清单明确 | 区分"新增"与"已执行" | 本轮新增仅 `99e_poi_seed.sql` | ⚠️ 描述曾混淆 |

> 结论预期：**部署项均为"未就绪"，但不影响本轮（工程）验收** —— 部署验收另开一轮。

---

## 统计表

| 组 | 用例数 | 通过 | 失败 | 已知失败（豁免） | 未测 |
|---|---|---|---|---|---|
| B-1 接口契约 | 19 | | | 1（分页 total） | |
| B-2 鉴权越权 | 9 | | | 0 | |
| B-3 错误码 | 9 | | | 2（空 body / period=abc） | |
| B-4 并发事务 | 7 | | | 0 | |
| B-5 缓存性能 | 9 | | | 0 | |
| B-6 AI 能力 | 21 | | | 3（负样本/DATA-01/DATA-02） | |
| B-7 数据层 | 8 | | | 0 | |
| B-8 异步任务 | 7 | | | 1（500 非契约） | 3 |
| B-9 安全 | 16 | | | 4（SEC-17/18/07/08 + P1 签名） | |
| B-10 工程化 | 8 | | | 1（无 CI） | |
| B-11 部署预检 | 8 | | | 6（未就绪） | |
| **合计** | **121** | | | **18** | **3** |

**门槛**：B-1 / B-2 / B-4 必须 **100%**（不含豁免项）；其余记录数值并说明。

---

## 已知失败项速查（验收时对照，避免误报为新缺陷）

| 编号 | 用例 | 问题 | 审计编号 | 是否阻断本轮 |
|---|---|---|---|---|
| 1 | B-1-16 | 分页 `total` = 本页条数 | `DATA-06` | 否（P2） |
| 2 | B-3-3 | 空 body → 422 非契约 | `ENG-xx` | 否 |
| 3 | B-3-4 | `period=abc` → 500 | `DATA-xx` | 否 |
| 4 | B-6-5 | 负样本误命中 100% | C20 待做 | 否（已排期） |
| 5 | B-6-13 | 二手/论坛读路径不过滤审核 | `DATA-01` | ⚠️ **建议必修** |
| 6 | B-6-14 | Agent 发布不过审 | `DATA-02` | ⚠️ **建议必修** |
| 7 | B-8-4 | `.delay()` 异常 → 500 | 新增（B15） | 否 |
| 8 | B-9-2 | XFF 限流绕过 | `SEC-17` P0 | ⚠️ **建议必修** |
| 9 | B-9-8 | 图片 403（签名闭环） | P1（PR42 未修） | ⚠️ **建议必修** |
| 10 | B-9-9 | 默认 JWT 密钥 | `SEC-18` P0 | 否（上线前必修） |
| 11 | B-9-11/12 | 登出/refresh 未失效 | `SEC-07/08` | 否（需改表） |
| 12 | B-10-6 | 无 CI | `ENG-01` | 否 |
| 13 | B-11-* | 部署未就绪 | `ENG-xx` | 否（另轮验收） |

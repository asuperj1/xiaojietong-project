# RAG 向量化依赖（Python 3.14 已验证兼容）
# 安装：pip install -r ai/rag/requirements.txt
# 说明：embedding 经 Ollama /api/embed 调用 bge-m3（无需本地下载模型文件）；
#       ChromaDB 未安装时 services/vector_store.py 自动降级为 numpy 内存向量库。

numpy>=2.0
chromadb>=1.5
httpx>=0.27

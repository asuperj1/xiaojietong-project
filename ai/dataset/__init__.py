"""数据集采集与标注工具链（`C31`）。

模块划分（各司其职，避免"一个脚本干完所有事"）：

| 模块 | 职责 |
|---|---|
| `schema.py` | **样本标准格式**与校验（唯一的真相来源） |
| `collect.py` | 采集：本地目录 / 数据库 → `Sample` 列表（含去重） |
| `prelabel.py` | 预标注：可插拔 labeler（默认轻量规则），产出可直接人工修订的初稿 |
| `export.py` | 导出：训练用 JSONL / 人工标注 CSV / 统计报告 Markdown |
| `cli.py` | 命令行串起上面四步 |

为什么要有 `schema` 这一层：采集、预标注、导出、人工修订是**四条链路**，
格式一旦漂移，下游的 `C32`（标注一致性 Kappa）与 `C33`（微调）都要返工。
"""

__all__ = ["schema", "collect", "prelabel", "export"]
